[
  {
    "objectID": "posts/welcome/index.html",
    "href": "posts/welcome/index.html",
    "title": "Welcome To My Blog",
    "section": "",
    "text": "Welcome! This blog is where I write up things I’m exploring in math, statistics, and machine learning — usually starting from a paper or an idea I want to understand better, and working through it in code until it’s concrete.\nExpect a mix of interactive explainers, reproductions of results from papers I find interesting, and the occasional deep dive into a dataset or method. Posts will lean toward showing the work rather than just stating conclusions, so there’s often code, data, and plots alongside the writing.\n\nThanks for stopping by — more soon."
  },
  {
    "objectID": "posts/diffusion-phase-transition/index.html",
    "href": "posts/diffusion-phase-transition/index.html",
    "title": "A Phase Transition in Diffusion Models",
    "section": "",
    "text": "Sclocchi, Favero and Wyart’s A Phase Transition in Diffusion Models Reveals the Hierarchical Nature of Data (Sclocchi, Favero, and Wyart 2024) makes a simple, sharp claim about how diffusion models generate images. Take a real image, run the forward noising process partway to some timestep \\(t\\), then run the backward (denoising) process the rest of the way back to \\(t=0\\). If \\(t\\) is small, you get back almost exactly the image you started with. If \\(t\\) is large, you get a plausible image of the same rough kind but with different fine details. The paper’s finding is that the switch between these two regimes isn’t gradual — it’s a genuine phase transition, and it happens at different noise levels for different levels of an image’s hierarchy (fine texture destabilizes first, then object parts, then overall semantic class).\nThis post reproduces that experiment end to end: real pretrained weights, real ImageNet-derived images, run locally on a Mac (Apple Silicon, no CUDA) rather than the multi-GPU cluster the paper used. The code lives in forward-backward-diffusion, the paper’s own release — I had to fix a couple of bugs in it and add Apple GPU (MPS) support to get it running here; the how and why of that is at the bottom of the post."
  },
  {
    "objectID": "posts/diffusion-phase-transition/index.html#scaling-it-down-to-run-on-a-laptop",
    "href": "posts/diffusion-phase-transition/index.html#scaling-it-down-to-run-on-a-laptop",
    "title": "A Phase Transition in Diffusion Models",
    "section": "Scaling it down to run on a laptop",
    "text": "Scaling it down to run on a laptop\nThe paper’s own experiment uses OpenAI’s 256×256 unconditional ADM diffusion model over the full ImageNet-1k validation set (1000 classes, 10 images each), resampled to 250 diffusion steps, swept across the full 0–250 range of reversal points. Doing that on a single Mac would take a very long time, so this reproduction keeps every piece of the pipeline real but turns the dials down:\n\n\n\n\nPaper\nThis post\n\n\n\n\nDiffusion model\n256×256 unconditional ADM (OpenAI)\nsame checkpoint\n\n\nClassifier\nConvNeXt-Base (torchvision, ImageNet-1k)\nsame model\n\n\nImages\nImageNet-1k validation, 1000 classes × 10\nImagenette validation, 10 classes × 3 (a real, license-free ImageNet subset)\n\n\nDiffusion steps (respaced)\n250\n25\n\n\nReversal points swept\n0–250 (fine grid)\n7 points: 3, 6, 10, 14, 18, 22, 25\n\n\nHardware\nGPU cluster\nApple M4 Pro, MPS backend, fp32\n\n\n\nSo: same model, same classifier, same underlying mechanism, real (if smaller) ImageNet-derived data — just a coarser grid and a tenth-scale image set, sized to finish in about 20 minutes on a laptop instead of hours on a cluster."
  },
  {
    "objectID": "posts/diffusion-phase-transition/index.html#where-does-the-class-identity-break",
    "href": "posts/diffusion-phase-transition/index.html#where-does-the-class-identity-break",
    "title": "A Phase Transition in Diffusion Models",
    "section": "Where does the class identity break?",
    "text": "Where does the class identity break?\nFor each reversal point \\(t\\), we forward-diffuse every starting image to \\(t\\), denoise it back to a clean image, and ask ConvNeXt-Base what class it thinks the result is — then compare that to what it originally said about the untouched image. sample_correct and start_correct track agreement with the true (Imagenette) label; pred_unchanged tracks whether reconstruction preserved the classifier’s own original prediction, which is the more direct read on “did the reverse process preserve identity” independent of whether the classifier was right to begin with.\n\nsummary_by_t &lt;- accuracy_by_t |&gt;\n  group_by(step_reverse) |&gt;\n  summarise(\n    sample_correct = mean(sample_correct),\n    pred_unchanged = mean(pred_unchanged),\n    .groups = \"drop\"\n  ) |&gt;\n  pivot_longer(c(sample_correct, pred_unchanged), names_to = \"metric\", values_to = \"fraction\")\n\nbaseline_acc &lt;- baseline$accuracy_top1[1] / 100\n\nggplot(summary_by_t, aes(step_reverse, fraction, color = metric)) +\n  geom_hline(yintercept = baseline_acc, linetype = \"dashed\", color = \"grey50\") +\n  annotate(\"text\", x = max(summary_by_t$step_reverse), y = baseline_acc,\n           label = \"baseline accuracy (no diffusion)\", vjust = -0.6, hjust = 1,\n           size = 3, color = \"grey50\") +\n  geom_line(linewidth = 1) +\n  geom_point(size = 2) +\n  scale_y_continuous(labels = percent_format(), limits = c(0, 1)) +\n  scale_color_manual(\n    values = c(sample_correct = \"#2C7FB8\", pred_unchanged = \"#D95F02\"),\n    labels = c(sample_correct = \"matches true label\", pred_unchanged = \"matches original prediction\")\n  ) +\n  labs(\n    x = \"reversal timestep (t, out of 25 respaced steps)\",\n    y = \"fraction of images\",\n    color = NULL,\n    title = \"Identity survives small t, then degrades\"\n  )\n\n\n\n\n\n\n\nFigure 1: Classification agreement after forward-backward diffusion, as a function of the reversal timestep. t=0 is a no-op (perfect reconstruction); higher t means more of the diffusion trajectory is re-randomized before reconstruction."
  },
  {
    "objectID": "posts/diffusion-phase-transition/index.html#a-hierarchy-of-breakdown-layer-by-layer",
    "href": "posts/diffusion-phase-transition/index.html#a-hierarchy-of-breakdown-layer-by-layer",
    "title": "A Phase Transition in Diffusion Models",
    "section": "A hierarchy of breakdown, layer by layer",
    "text": "A hierarchy of breakdown, layer by layer\nThe more interesting view is inside the classifier. Every image gets pushed through ConvNeXt-Base twice — once as the original, once as the forward-backward reconstruction — and we compare activations at every one of its ~45 internal layers, from the earliest convolutional stem (depth 0) to the final classification head (depth ≈44). If the paper’s hierarchical story is right, shallow layers (fine texture) should start diverging at small \\(t\\), while only the deepest layers (semantic class) diverge at large \\(t\\).\n\ncosine_by_depth_t &lt;- activation_diff |&gt;\n  filter(metric == \"cosine\") |&gt;\n  group_by(step_reverse, depth) |&gt;\n  summarise(cosine_distance = 1 - mean(value), .groups = \"drop\")\n\nggplot(cosine_by_depth_t, aes(step_reverse, depth, fill = cosine_distance)) +\n  geom_tile() +\n  scale_fill_viridis_c(option = \"magma\", labels = percent_format()) +\n  scale_y_continuous(breaks = pretty_breaks()) +\n  labs(\n    x = \"reversal timestep (t)\",\n    y = \"network depth (0 = input stem, ~44 = classification head)\",\n    fill = \"cosine\\ndistance\",\n    title = \"Where in the network reconstruction diverges from the original\"\n  )\n\n\n\n\n\n\n\nFigure 2: Mean cosine distance (1 - cosine similarity) between original and reconstructed activations, by network depth and reversal timestep. Warmer = more disrupted. A hierarchy would show the disruption front moving from shallow layers (bottom) to deep layers (top) as t increases.\n\n\n\n\n\n\ndepth_max &lt;- max(cosine_by_depth_t$depth)\nsample_depths &lt;- unique(round(seq(0, depth_max, length.out = 6)))\n\ncosine_by_depth_t |&gt;\n  filter(depth %in% sample_depths) |&gt;\n  mutate(depth = factor(depth)) |&gt;\n  ggplot(aes(step_reverse, cosine_distance, color = depth, group = depth)) +\n  geom_line(linewidth = 1) +\n  geom_point(size = 1.5) +\n  scale_color_viridis_d(option = \"magma\", end = 0.85) +\n  scale_y_continuous(labels = percent_format()) +\n  labs(\n    x = \"reversal timestep (t)\",\n    y = \"cosine distance\",\n    color = \"depth\",\n    title = \"Shallow layers move first, deep layers move last\"\n  )\n\n\n\n\n\n\n\nFigure 3: The same data as a line plot: a handful of representative depths, cosine distance vs. t."
  },
  {
    "objectID": "posts/diffusion-phase-transition/index.html#seeing-it-directly",
    "href": "posts/diffusion-phase-transition/index.html#seeing-it-directly",
    "title": "A Phase Transition in Diffusion Models",
    "section": "Seeing it directly",
    "text": "Seeing it directly\nNumbers aside, the clearest evidence is just looking at the images. Below, each row is one starting image; each column is the reconstruction after forward-backward diffusion at an increasing reversal timestep. At small \\(t\\) the reconstruction is nearly pixel-identical to the original. At larger \\(t\\), fine texture is the first thing to change, followed eventually by pose, composition, and — at the largest \\(t\\) we tested — occasionally the object itself.\n\nlibrary(jpeg)\nlibrary(grid)\n\nmanifest &lt;- read_csv(\"data/image_grid_manifest.csv\", show_col_types = FALSE)\nt_values &lt;- sort(unique(manifest$step_reverse[manifest$step_reverse &gt; 0]))\nimage_ids &lt;- unique(manifest$image_id)\n\ngrid.newpage()\nn_rows &lt;- length(image_ids)\nn_cols &lt;- length(t_values) + 1\npushViewport(viewport(layout = grid.layout(\n  n_rows + 1, n_cols,\n  heights = unit(c(1.5, rep(5, n_rows)), c(\"lines\", rep(\"null\", n_rows)))\n)))\n\ndraw_label &lt;- function(label, col) {\n  pushViewport(viewport(layout.pos.row = 1, layout.pos.col = col))\n  grid.text(label, gp = gpar(fontsize = 9))\n  popViewport()\n}\n\ndraw_cell &lt;- function(path, row, col) {\n  img &lt;- readJPEG(path)\n  pushViewport(viewport(layout.pos.row = row + 1, layout.pos.col = col))\n  grid.raster(img)\n  popViewport()\n}\n\ndraw_label(\"original\", 1)\nfor (c in seq_along(t_values)) draw_label(paste0(\"t=\", t_values[c]), c + 1)\n\nfor (r in seq_along(image_ids)) {\n  row_data &lt;- manifest |&gt; filter(image_id == image_ids[r])\n  start_path &lt;- row_data$path[row_data$step_reverse == 0]\n  draw_cell(start_path, r, 1)\n  for (c in seq_along(t_values)) {\n    t_path &lt;- row_data$path[row_data$step_reverse == t_values[c]]\n    if (length(t_path) == 1) {\n      draw_cell(t_path, r, c + 1)\n    }\n  }\n}\n\n\n\n\n\n\n\nFigure 4: Original (leftmost column) vs. forward-backward reconstructions at increasing t."
  },
  {
    "objectID": "posts/diffusion-phase-transition/index.html#what-actually-broke-in-the-code-and-how-i-fixed-it",
    "href": "posts/diffusion-phase-transition/index.html#what-actually-broke-in-the-code-and-how-i-fixed-it",
    "title": "A Phase Transition in Diffusion Models",
    "section": "What actually broke in the code, and how I fixed it",
    "text": "What actually broke in the code, and how I fixed it\nThe paper’s released code isn’t quite runnable as committed, and separately, nothing about it was written with Apple’s MPS backend in mind. Four fixes were needed:\n\nA broken relative import. guided_diffusion/image_datasets.py did from ..datasets.imagenet_classes import IMAGENET_CLASSES, a relative import that reaches outside the installed guided_diffusion package. It fails immediately with ImportError: attempted relative import beyond top-level package — every one of the repo’s three custom scripts imports this module, so nothing in the repo could run at all. Fixed by making datasets its own installable package (setup.py) and importing it absolutely.\nTwo sampling methods that don’t exist. The actual forward-backward experiment script calls diffusion.p_sample_loop_forw_back and diffusion.ddim_sample_loop_forw_back — methods that appear nowhere in gaussian_diffusion.py, and never have, across the entire git history. I implemented both, as variants of the existing p_sample_loop_progressive / ddim_sample_loop_progressive that start the reverse process at a given timestep instead of at pure noise.\nfloat64 on MPS. _extract_into_tensor moved the diffusion schedule’s coefficients — stored as float64 numpy arrays — directly onto the compute device. MPS doesn’t support float64 at all, so this crashed on first use; fixed by casting to float32 before the device transfer.\ntorch.distributed collectives on an MPS tensor. The scripts use dist.all_reduce/all_gather (inherited from the original multi-GPU design) even for a single-process laptop run. Those collectives run over the gloo backend here (no CUDA means no NCCL), and gloo only understands CPU tensors — so the handful of tensors that cross a collective call needed to be routed to CPU rather than MPS, while leaving NCCL’s CUDA path untouched for the cluster case.\n\nNone of this changes the paper’s method — it’s the same forward-then-partial-backward diffusion process, the same classifier, the same activation-comparison logic. The fixes just make the released code actually executable, and executable somewhere other than a CUDA cluster.\nRelated: Fixed Points, Phase Transitions, and Diffusion Models connects this paper’s belief-propagation phase transition to the renormalization-group critical phenomena in my own dissertation · if any of the underlying math felt assumed rather than explained, Markov Chains, Concretely and A Diffusion Model You Can Actually Solve are refreshers written to fill exactly that gap."
  },
  {
    "objectID": "posts/diffusion-phase-transition/index.html#references",
    "href": "posts/diffusion-phase-transition/index.html#references",
    "title": "A Phase Transition in Diffusion Models",
    "section": "References",
    "text": "References\n\n\nSclocchi, Antonio, Alessandro Favero, and Matthieu Wyart. 2024. “A Phase Transition in Diffusion Models Reveals the Hierarchical Nature of Data.” arXiv Preprint arXiv:2402.16991."
  },
  {
    "objectID": "papers/instrumenting-agent-cybernetics/index.html",
    "href": "papers/instrumenting-agent-cybernetics/index.html",
    "title": "Instrumenting Agent Cybernetics",
    "section": "",
    "text": "Wang et al. (2026) argue that cybernetics supplies the missing theoretical scaffold for foundation agents, and derive six design principles from six classical laws. Their Principle 2 (Requisite Variety) requires that \\(\\min(V(\\mathcal{O}), V(\\mathcal{I})) \\geq V(\\mathcal{W})\\), and three of the failure modes they name are diagnosed as \\(V(\\mathcal{T}) &lt; V(\\mathcal{W})\\). Their headline open question — how should an agent respond when its environment exceeds its representational capacity? — presupposes that this condition can be detected.\nIt cannot, as the framework currently stands. \\(V(\\mathcal{W})\\) appears throughout as Ashby’s classical cardinality — a theoretical quantity Wang et al. never operationalise from deployment telemetry. An operator who tries to estimate it from observed disturbance classes defaults to plug-in (maximum-likelihood) coverage; that default systematically understates environmental variety and overstates coverage, worst where the distribution is heaviest-tailed. Agent Cybernetics is, as written, unmeasurable.\nWe supply the instrument. Our central result is the Enumeration Elasticity Theorem: the elasticity of unhandled traffic with respect to enumeration investment is exactly \\(\\eta = 1 - \\beta\\), where \\(\\beta\\) is the Heaps exponent of the disturbance-class discovery curve — a directly measurable quantity. A \\(\\lambda\\)-fold increase in the effort spent mapping disturbance classes to responses reduces the unhandled fraction by \\(\\lambda^{1-\\beta}\\). As \\(\\beta \\to 1\\), \\(\\eta \\to 0\\) and investment stops buying coverage: at \\(\\beta = 0.9\\), halving the residual costs a thousandfold increase in effort. This gives an operational answer to Wang et al.’s open question. When \\(\\eta \\approx 0\\), requisite variety cannot be met by amplification at any ledger a rational operator will build, and the deficit must be met by attenuation or by a class-independent fallback — an architectural conclusion, not an engineering one.\nWe further contribute: an open-world estimator for \\(V(\\mathcal{W})\\) combining Good–Turing missing-mass correction and Heaps tracking, and a demonstration that plug-in estimation errs on both sides of the inequality at once; a determinism instrument, \\(H(R \\mid D)\\), that operationalises the Conant–Ashby good regulator theorem, which is absent from the six laws; an interventional test that renders feedback blindness — a named closed-loop reliability failure they diagnose but do not measure (one of their first four failure modes, ranked highest for cost–benefit) — falsifiable rather than merely diagnosable, and which we run on the demonstrator (\\(\\Phi = 0.55\\) bits, \\(\\approx\\) 90x the resampling-noise floor: the agent’s action distribution genuinely conditions on its observations); and a partition-refinement procedure with three admission gates, including a policy-improvement criterion that corrects a natural but wrong instinct about when a disturbance class should be split.\nWe also identify a hazard in our own instrument: attenuation lowers measured \\(\\beta\\), so the treatment we recommend improves the diagnostic whether or not it improves the system. We give the mitigation and do not claim to resolve it.\nFinally, the same interventional machinery yields an adversarial instrument: susceptibility \\(\\Sigma\\), the probability that an agent complies with an instruction injected into its observation channel (indirect prompt poisoning). Measured end-to-end through an agent’s real data-input tools — file read, RAG retrieval, web fetch — against a matched clean control (\\(\\Sigma_{\\text{clean}} = 0\\)), \\(\\Sigma\\) is strongly attack-class-dependent (0.83 for a blunt instruction-override, 0.00 for a system-prompt-exfiltration attempt) and inversely tracks content-sensitivity \\(\\Phi\\) across vectors, the direction §5.2 predicts: an agent that treats tool output as evidence to be checked, not an instruction to obey, is harder to hijack through that output, while a feedback-blind one passes an injected payload straight through.\nWe package these as a variety ledger: a versioned registry of essential variables, disturbance classes, response repertoire, and channel specifications, recomputed from telemetry and enforced as a CI gate. §6 exercises the full ledger on a semi-synthetic benchmark with known ground truth — including a graded outcome signal that makes the partition-refinement gates non-vacuous, an interventional feedback-blindness measurement, and an end-to-end prompt-poisoning susceptibility measurement — and reports one incident: a latent regulatory defect that inverted the dithering measurement until it was found and fixed, which is itself the clearest available demonstration of why §4.5’s canary discipline is not optional.\nDOI: 10.13140/RG.2.2.32031.11681\nDownload the PDF"
  },
  {
    "objectID": "papers/instrumenting-agent-cybernetics/index.html#abstract",
    "href": "papers/instrumenting-agent-cybernetics/index.html#abstract",
    "title": "Instrumenting Agent Cybernetics",
    "section": "",
    "text": "Wang et al. (2026) argue that cybernetics supplies the missing theoretical scaffold for foundation agents, and derive six design principles from six classical laws. Their Principle 2 (Requisite Variety) requires that \\(\\min(V(\\mathcal{O}), V(\\mathcal{I})) \\geq V(\\mathcal{W})\\), and three of the failure modes they name are diagnosed as \\(V(\\mathcal{T}) &lt; V(\\mathcal{W})\\). Their headline open question — how should an agent respond when its environment exceeds its representational capacity? — presupposes that this condition can be detected.\nIt cannot, as the framework currently stands. \\(V(\\mathcal{W})\\) appears throughout as Ashby’s classical cardinality — a theoretical quantity Wang et al. never operationalise from deployment telemetry. An operator who tries to estimate it from observed disturbance classes defaults to plug-in (maximum-likelihood) coverage; that default systematically understates environmental variety and overstates coverage, worst where the distribution is heaviest-tailed. Agent Cybernetics is, as written, unmeasurable.\nWe supply the instrument. Our central result is the Enumeration Elasticity Theorem: the elasticity of unhandled traffic with respect to enumeration investment is exactly \\(\\eta = 1 - \\beta\\), where \\(\\beta\\) is the Heaps exponent of the disturbance-class discovery curve — a directly measurable quantity. A \\(\\lambda\\)-fold increase in the effort spent mapping disturbance classes to responses reduces the unhandled fraction by \\(\\lambda^{1-\\beta}\\). As \\(\\beta \\to 1\\), \\(\\eta \\to 0\\) and investment stops buying coverage: at \\(\\beta = 0.9\\), halving the residual costs a thousandfold increase in effort. This gives an operational answer to Wang et al.’s open question. When \\(\\eta \\approx 0\\), requisite variety cannot be met by amplification at any ledger a rational operator will build, and the deficit must be met by attenuation or by a class-independent fallback — an architectural conclusion, not an engineering one.\nWe further contribute: an open-world estimator for \\(V(\\mathcal{W})\\) combining Good–Turing missing-mass correction and Heaps tracking, and a demonstration that plug-in estimation errs on both sides of the inequality at once; a determinism instrument, \\(H(R \\mid D)\\), that operationalises the Conant–Ashby good regulator theorem, which is absent from the six laws; an interventional test that renders feedback blindness — a named closed-loop reliability failure they diagnose but do not measure (one of their first four failure modes, ranked highest for cost–benefit) — falsifiable rather than merely diagnosable, and which we run on the demonstrator (\\(\\Phi = 0.55\\) bits, \\(\\approx\\) 90x the resampling-noise floor: the agent’s action distribution genuinely conditions on its observations); and a partition-refinement procedure with three admission gates, including a policy-improvement criterion that corrects a natural but wrong instinct about when a disturbance class should be split.\nWe also identify a hazard in our own instrument: attenuation lowers measured \\(\\beta\\), so the treatment we recommend improves the diagnostic whether or not it improves the system. We give the mitigation and do not claim to resolve it.\nFinally, the same interventional machinery yields an adversarial instrument: susceptibility \\(\\Sigma\\), the probability that an agent complies with an instruction injected into its observation channel (indirect prompt poisoning). Measured end-to-end through an agent’s real data-input tools — file read, RAG retrieval, web fetch — against a matched clean control (\\(\\Sigma_{\\text{clean}} = 0\\)), \\(\\Sigma\\) is strongly attack-class-dependent (0.83 for a blunt instruction-override, 0.00 for a system-prompt-exfiltration attempt) and inversely tracks content-sensitivity \\(\\Phi\\) across vectors, the direction §5.2 predicts: an agent that treats tool output as evidence to be checked, not an instruction to obey, is harder to hijack through that output, while a feedback-blind one passes an injected payload straight through.\nWe package these as a variety ledger: a versioned registry of essential variables, disturbance classes, response repertoire, and channel specifications, recomputed from telemetry and enforced as a CI gate. §6 exercises the full ledger on a semi-synthetic benchmark with known ground truth — including a graded outcome signal that makes the partition-refinement gates non-vacuous, an interventional feedback-blindness measurement, and an end-to-end prompt-poisoning susceptibility measurement — and reports one incident: a latent regulatory defect that inverted the dithering measurement until it was found and fixed, which is itself the clearest available demonstration of why §4.5’s canary discipline is not optional.\nDOI: 10.13140/RG.2.2.32031.11681\nDownload the PDF"
  },
  {
    "objectID": "papers/instrumenting-agent-cybernetics/index.html#read-it-here",
    "href": "papers/instrumenting-agent-cybernetics/index.html#read-it-here",
    "title": "Instrumenting Agent Cybernetics",
    "section": "Read it here",
    "text": "Read it here"
  },
  {
    "objectID": "papers/instrumenting-agent-cybernetics/index.html#cite-this-paper",
    "href": "papers/instrumenting-agent-cybernetics/index.html#cite-this-paper",
    "title": "Instrumenting Agent Cybernetics",
    "section": "Cite this paper",
    "text": "Cite this paper\n@misc{milne2026instrumenting,\n  title        = {Instrumenting Agent Cybernetics: Open-World Estimation of Requisite Variety in Deployed Agents},\n  author       = {Milne, Paul},\n  year         = {2026},\n  doi          = {10.13140/RG.2.2.32031.11681},\n  note         = {Independent researcher}\n}"
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "math-blog",
    "section": "",
    "text": "A Phase Transition in Diffusion Models\n\n\n\n\n\n\ndiffusion\n\ndeep-learning\n\nreproduction\n\n\n\nReproducing ‘A Phase Transition in Diffusion Models Reveals the Hierarchical Nature of Data’ (Sclocchi, Favero & Wyart 2024) locally on Apple Silicon.\n\n\n\n\n\nSep 6, 2026\n\n\nPaul Milne\n\n\n\n\n\n\n\n\n\n\n\n\nFixed Points, Phase Transitions, and Diffusion Models\n\n\nA functional-equation bridge between renormalization-group critical phenomena and the hierarchical structure of denoising diffusion\n\n\n\ndiffusion\n\ndeep-learning\n\nstatistical-mechanics\n\nrenormalization-group\n\nreproduction\n\n\n\nImplementing in R the two central bifurcation maps that connect Kadanoff block-spin renormalization group theory to belief propagation in hierarchical diffusion models.\n\n\n\n\n\nSep 6, 2026\n\n\nPaul Milne\n\n\n\n\n\n\n\n\n\n\n\n\nWelcome To My Blog\n\n\n\n\n\n\nnews\n\n\n\nAn introduction to this blog: math, statistics, and machine learning explored through code and paper reproductions.\n\n\n\n\n\nJun 10, 2026\n\n\nPaul Milne\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "explainers/mean-field-annealed-disorder/index.html",
    "href": "explainers/mean-field-annealed-disorder/index.html",
    "title": "Mean-Field Theory and Annealed Disorder",
    "section": "",
    "text": "Second of three short refreshers behind Reproducing a Phase Transition in Diffusion Models and Fixed Points, Phase Transitions, and Diffusion Models. The first covered Markov chains; read it first only if that material feels unfamiliar — this one stands alone."
  },
  {
    "objectID": "explainers/mean-field-annealed-disorder/index.html#the-problem-mean-field-theory-solves",
    "href": "explainers/mean-field-annealed-disorder/index.html#the-problem-mean-field-theory-solves",
    "title": "Mean-Field Theory and Annealed Disorder",
    "section": "The problem mean-field theory solves",
    "text": "The problem mean-field theory solves\nA ferromagnet is a lattice of interacting spins: each one wants to align with its neighbours, but the neighbours are themselves fluctuating, coupled to their neighbours, and so on. Tracking the full joint distribution over every spin is intractable for anything but tiny systems.\nThe mean-field trick: replace each spin’s actual, fluctuating neighbours with their average value. A many-body problem becomes a single spin sitting in an effective field set by everybody else’s average behaviour — and that’s tractable, at the cost of ignoring fluctuation correlations between neighbours. Sclocchi et al. (2024) (Sclocchi, Favero, and Wyart 2024) use exactly this move — under a different name, “annealed” approximation — to get a closed-form description of belief propagation on their Random Hierarchy Model, and it’s worth seeing the classical version first."
  },
  {
    "objectID": "explainers/mean-field-annealed-disorder/index.html#curie-weiss-derived",
    "href": "explainers/mean-field-annealed-disorder/index.html#curie-weiss-derived",
    "title": "Mean-Field Theory and Annealed Disorder",
    "section": "Curie-Weiss, derived",
    "text": "Curie-Weiss, derived\nGive each spin \\(z\\) neighbours, coupling strength \\(J\\), at inverse temperature \\(\\beta\\). Averaging over its neighbours’ fluctuations, a spin’s effective field is proportional to the mean magnetization \\(m\\), and self-consistency (the average spin must reproduce the magnetization that produced its own field) gives:\n\\[\nm = \\tanh(Km), \\qquad K \\equiv \\beta J z\n\\]\nFolding \\(\\beta\\), \\(J\\), \\(z\\) into the single parameter \\(K\\) is the standard textbook reduction — the actual critical temperature is \\(\\beta_c = 1/(Jz)\\), i.e. \\(K=1\\) exactly. Below \\(K=1\\) the only solution is \\(m=0\\) (paramagnetic: no net alignment); above it, two new nonzero solutions appear (ferromagnetic: spontaneous alignment, sign chosen by symmetry breaking)."
  },
  {
    "objectID": "explainers/mean-field-annealed-disorder/index.html#solving-it",
    "href": "explainers/mean-field-annealed-disorder/index.html#solving-it",
    "title": "Mean-Field Theory and Annealed Disorder",
    "section": "Solving it",
    "text": "Solving it\n\\(m=0\\) satisfies \\(\\tanh(K \\cdot 0) = 0\\) for every \\(K\\) — it’s always a trivial root, present whether or not the interesting nonzero one exists. Searching for the nonzero branch means excluding it explicitly: bracket uniroot away from zero.\n\nnontrivial_root &lt;- function(K, lower = 1e-6, upper = 1 - 1e-9) {\n  if (K &lt;= 1) return(0)\n  self_consist_gap &lt;- function(m) tanh(K * m) - m\n  if (sign(self_consist_gap(lower)) == sign(self_consist_gap(upper))) return(NA_real_)\n  uniroot(self_consist_gap, c(lower, upper))$root\n}\n\nKs &lt;- seq(0.05, 3, by = 0.01)\ndf &lt;- data.frame(K = Ks)\ndf$m_stable &lt;- sapply(Ks, nontrivial_root)\n\nggplot(df, aes(K)) +\n  geom_hline(yintercept = 0, colour = \"grey60\", linetype = \"dashed\") +\n  geom_line(aes(y = m_stable), colour = \"firebrick\", linewidth = 1, na.rm = TRUE) +\n  geom_line(aes(y = -m_stable), colour = \"firebrick\", linewidth = 1, na.rm = TRUE) +\n  geom_vline(xintercept = 1, linetype = \"dotted\") +\n  labs(x = expression(K == beta * J * z), y = \"magnetization m\",\n       title = \"Order emerges above the critical point\")\n\n\n\n\n\n\n\nFigure 1: Curie-Weiss magnetization vs. K. The nonzero branch only exists above the critical point K=1.\n\n\n\n\n\nBelow \\(K=1\\), only \\(m=0\\); above it, a genuine phase transition. Three independent checks on the nonzero branch agree: at \\(K=2\\), uniroot gives \\(m=0.9575\\), and simply iterating \\(m \\leftarrow \\tanh(2m)\\) a thousand times from \\(m=0.5\\) converges to \\(0.957504\\) — the same fixed point found two different ways. Near the critical point, mean-field theory predicts \\(m \\approx \\sqrt{3(K-1)}\\); at \\(K=1.01\\) that asymptotic gives \\(0.1732\\) against uniroot’s \\(0.1717\\) — agreement to about 1%, which is the classical mean-field critical exponent \\(\\beta=1/2\\) showing up numerically."
  },
  {
    "objectID": "explainers/mean-field-annealed-disorder/index.html#the-same-move-in-belief-propagation",
    "href": "explainers/mean-field-annealed-disorder/index.html#the-same-move-in-belief-propagation",
    "title": "Mean-Field Theory and Annealed Disorder",
    "section": "The same move, in belief propagation",
    "text": "The same move, in belief propagation\nThe Random Hierarchy Model’s disordered production rules play the role of the Ising model’s disordered neighbour spins. Tracking every possible realization of the hierarchy’s rules exactly is just as intractable as tracking every spin’s actual neighbours. Sclocchi et al. take the identical shortcut: average over the ensemble of rule realizations (the “annealed” approximation) rather than conditioning on one specific (quenched) realization, and a complicated random recursion collapses into the single deterministic belief-propagation map\n\\[\np' = F(p)\n\\]\nalready implemented and solved in Fixed Points, Phase Transitions, and Diffusion Models — same recipe (disorder → ensemble average → deterministic self-consistency map), same qualitative payoff (a clean, analyzable bifurcation), different physical dressing. That post doesn’t re-derive the annealed approximation; this is the missing piece that justifies why \\(F(p)\\) is allowed to be written down as a plain deterministic function at all.\nThe same self-consistency structure — and the same \\(\\sqrt{K-1}\\)-type critical scaling — also shows up throughout my own dissertation on renormalization-group critical phenomena, where the RG fixed point plays the role \\(K=1\\) plays here."
  },
  {
    "objectID": "explainers/mean-field-annealed-disorder/index.html#whats-next",
    "href": "explainers/mean-field-annealed-disorder/index.html#whats-next",
    "title": "Mean-Field Theory and Annealed Disorder",
    "section": "What’s next",
    "text": "What’s next\nThe final post in this series builds a complete toy diffusion model from scratch — forward noising, the score function, and exact reverse sampling — using the closed-form Markov chain from the first post."
  },
  {
    "objectID": "explainers/mean-field-annealed-disorder/index.html#references",
    "href": "explainers/mean-field-annealed-disorder/index.html#references",
    "title": "Mean-Field Theory and Annealed Disorder",
    "section": "References",
    "text": "References\n\n\nSclocchi, Antonio, Alessandro Favero, and Matthieu Wyart. 2024. “A Phase Transition in Diffusion Models Reveals the Hierarchical Nature of Data.” arXiv Preprint arXiv:2402.16991."
  },
  {
    "objectID": "explainers/index.html",
    "href": "explainers/index.html",
    "title": "Explainers",
    "section": "",
    "text": "Background explanations of the math and stats concepts that come up in the paper-reproduction posts — the kind of thing you’d want a refresher on before the main post assumes it.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nMarkov Chains, Concretely\n\n\nThe backbone of diffusion models — first of a three-part refresher series\n\n\n\nexplainer\n\ndiffusion\n\ndeep-learning\n\n\n\nA worked introduction to Markov chains — transition matrices, stationary distributions, and the Gaussian forward chain at the heart of DDPM — verified with R simulation and closed-form checks.\n\n\n\n\n\nSep 6, 2026\n\n\nPaul Milne\n\n\n\n\n\n\n\n\n\n\n\n\nMean-Field Theory and Annealed Disorder\n\n\nFrom Curie-Weiss to belief propagation — second of a three-part refresher series\n\n\n\nexplainer\n\nstatistical-mechanics\n\nrenormalization-group\n\n\n\nHow replacing a disordered quantity by its ensemble average turns an intractable many-body problem into a solvable self-consistency equation, worked through the classic Curie-Weiss ferromagnet and connected to belief propagation on the Random Hierarchy Model.\n\n\n\n\n\nSep 6, 2026\n\n\nPaul Milne\n\n\n\n\n\n\n\n\n\n\n\n\nA Diffusion Model You Can Actually Solve\n\n\nForward noise, the score function, and exact reverse sampling — third of a three-part refresher series\n\n\n\nexplainer\n\ndiffusion\n\ndeep-learning\n\n\n\nBuilding the smallest possible working diffusion model, a two-component 1D Gaussian mixture with an exact closed-form score, to make forward noising, Tweedie’s formula, and reverse ancestral sampling fully concrete and verifiable.\n\n\n\n\n\nSep 6, 2026\n\n\nPaul Milne\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "about.html",
    "href": "about.html",
    "title": "About",
    "section": "",
    "text": "About this blog"
  },
  {
    "objectID": "eclipse_2026_sun_overlay.html",
    "href": "eclipse_2026_sun_overlay.html",
    "title": "Projecting the sun’s path for the 12 August 2026 UK partial solar eclipse",
    "section": "",
    "text": "Note\n\n\n\nThis report was generated using AI under general human direction. At the time of generation, the contents have not been comprehensively reviewed by a human analyst."
  },
  {
    "objectID": "eclipse_2026_sun_overlay.html#overview",
    "href": "eclipse_2026_sun_overlay.html#overview",
    "title": "Projecting the sun’s path for the 12 August 2026 UK partial solar eclipse",
    "section": "Overview",
    "text": "Overview\nThis report projects the sun’s calculated azimuth/altitude path during the 12 August 2026 partial solar eclipse onto two images taken near 52.8936°N, 1.2659°W (Nottinghamshire/Lincolnshire border, UK), and separately visualizes the local terrain to give context on nearby high ground:\n\nA Google Street View image, aimed toward the sun’s position at peak eclipse.\nA photo taken with the Theodolite app, whose EXIF/XMP metadata records the camera’s GPS position, compass heading, and pitch.\nA 3D terrain surface of the surrounding 10-mile radius, with nearby named hills labeled and the local river network draped over the relief.\n\nBoth overlays use the same sun-path calculation and a small-angle (linear) projection from angular offset to pixel offset around the camera’s heading/pitch — not a true perspective projection. This is a reasonable approximation over the modest angular ranges involved here, but it is not exact, especially toward the edges of a wide field of view.\n\nlibrary(suncalc)\nlibrary(dplyr)\n\n\nAttaching package: 'dplyr'\n\n\nThe following objects are masked from 'package:stats':\n\n    filter, lag\n\n\nThe following objects are masked from 'package:base':\n\n    intersect, setdiff, setequal, union\n\nlibrary(lubridate)\n\n\nAttaching package: 'lubridate'\n\n\nThe following objects are masked from 'package:base':\n\n    date, intersect, setdiff, union\n\nlibrary(ggplot2)\nlibrary(jpeg)\nlibrary(httr)\nlibrary(exiftoolr)\nlibrary(sf)\n\nLinking to GEOS 3.13.0, GDAL 3.8.5, PROJ 9.5.1; sf_use_s2() is TRUE\n\nlibrary(terra)\n\nterra 1.9.34\n\nlibrary(elevatr)\n\nelevatr v0.99.0 NOTE: Version 0.99.0 of 'elevatr' uses 'sf' and 'terra'.  Use \nof the 'sp', 'raster', and underlying 'rgdal' packages by 'elevatr' is being \ndeprecated; however, get_elev_raster continues to return a RasterLayer.  This \nwill be dropped in future versions, so please plan accordingly.\n\nlibrary(osmdata)\n\nData (c) OpenStreetMap contributors, ODbL 1.0. https://www.openstreetmap.org/copyright\n\nlibrary(plotly)\n\n\nAttaching package: 'plotly'\n\n\nThe following object is masked from 'package:httr':\n\n    config\n\n\nThe following object is masked from 'package:ggplot2':\n\n    last_plot\n\n\nThe following object is masked from 'package:stats':\n\n    filter\n\n\nThe following object is masked from 'package:graphics':\n\n    layout\n\nlibrary(rayshader)\n\nWarning in rgl.init(initValue, onlyNULL): X11 error: GLXBadContext\n\n\nWarning: 'rgl.init' failed, will use the null device.\nSee '?rgl.useNULL' for ways to avoid this warning.\n\nlibrary(curl)\n\nUsing libcurl 8.14.1 with LibreSSL/3.3.6 \n\n\n\nAttaching package: 'curl'\n\n\nThe following object is masked from 'package:httr':\n\n    handle_reset"
  },
  {
    "objectID": "eclipse_2026_sun_overlay.html#eclipse-context-and-sun-path",
    "href": "eclipse_2026_sun_overlay.html#eclipse-context-and-sun-path",
    "title": "Projecting the sun’s path for the 12 August 2026 UK partial solar eclipse",
    "section": "Eclipse context and sun path",
    "text": "Eclipse context and sun path\n\nlat &lt;- 52.893608\nlon &lt;- -1.265934\ndate &lt;- as.Date(\"2026-08-12\")\n\n# Eclipse locally runs roughly 18:00-20:00 BST (17:00-19:00 UTC);\n# widen slightly either side to give context on the sun's path\ntimes &lt;- seq(\n  from = as.POSIXct(paste(date, \"16:30:00\"), tz = \"UTC\"),\n  to   = as.POSIXct(paste(date, \"19:15:00\"), tz = \"UTC\"),\n  by   = \"1 min\"\n)\n\nsun_path &lt;- getSunlightPosition(date = times, lat = lat, lon = lon) |&gt;\n  mutate(\n    # Convert azimuth (radians south-to-west) to compass degrees (0-360 from North)\n    azimuth_deg = (azimuth * 180 / pi + 180) %% 360,\n    altitude_deg = altitude * 180 / pi,\n    time_bst = format(with_tz(date, \"Europe/London\"), \"%H:%M\")\n  )\n\n# Reported UK-wide maximum eclipse is ~19:05-19:13 BST (18:05-18:13 UTC);\n# pick the row closest to 19:09 BST (18:09 UTC) for this longitude\npeak_time &lt;- as.POSIXct(paste(date, \"18:09:00\"), tz = \"UTC\")\npeak_sun &lt;- sun_path[which.min(abs(sun_path$date - peak_time)), ]\n\nAt peak eclipse (19:09 BST), the sun is at an azimuth of 278.9° and an altitude of 12.1°. Across the wider window shown here (16:30-19:15 UTC / 17:30-20:15 BST), azimuth ranges from 258.9° to 291.7°, and altitude from 2.5° to 27°.\nA shared helper converts an azimuth/altitude offset from a camera’s heading/pitch into pixel coordinates, given the image size and field of view:\n\nwrap180 &lt;- function(x) ((x + 180) %% 360) - 180\n\nproject_sun &lt;- function(az, alt, heading, pitch, fov_h, fov_v, img_w, img_h) {\n  delta_az &lt;- wrap180(az - heading)\n  delta_alt &lt;- alt - pitch\n  data.frame(\n    x = img_w / 2 + (delta_az / (fov_h / 2)) * (img_w / 2),\n    y = img_h / 2 + (delta_alt / (fov_v / 2)) * (img_h / 2),\n    delta_az = delta_az,\n    delta_alt = delta_alt\n  )\n}"
  },
  {
    "objectID": "eclipse_2026_sun_overlay.html#street-view-overlay",
    "href": "eclipse_2026_sun_overlay.html#street-view-overlay",
    "title": "Projecting the sun’s path for the 12 August 2026 UK partial solar eclipse",
    "section": "Street View overlay",
    "text": "Street View overlay\nThe Street View camera is pointed toward the sun’s position at peak eclipse (heading 278.9°, pitch 12.1°), using the default 90° horizontal field of view for a square image.\n\n# Cache network responses on disk so repeated re-renders (e.g. while\n# iterating on this document) don't repeatedly hit external APIs and risk\n# tripping their rate limits.\ncache_dir &lt;- \".cache\"\ndir.create(cache_dir, showWarnings = FALSE)\n\nsv_size &lt;- c(640, 640)\nsv_fov &lt;- 90\nsv_heading &lt;- peak_sun$azimuth_deg\nsv_pitch &lt;- peak_sun$altitude_deg\n\nsv_cache_file &lt;- file.path(\n  cache_dir,\n  sprintf(\"streetview_%s_%s_h%.1f_p%.1f_f%d_%dx%d.jpg\",\n          lat, lon, sv_heading, sv_pitch, sv_fov, sv_size[1], sv_size[2])\n)\n\nif (!file.exists(sv_cache_file)) {\n  api_key &lt;- Sys.getenv(\"GOOGLE_MAPS_KEY\")\n\n  sv_url &lt;- modify_url(\n    \"https://maps.googleapis.com/maps/api/streetview\",\n    query = list(\n      location = paste(lat, lon, sep = \",\"),\n      size     = paste(sv_size, collapse = \"x\"),\n      heading  = sv_heading,\n      fov      = sv_fov,\n      pitch    = sv_pitch,\n      key      = api_key\n    )\n  )\n\n  sv_resp &lt;- GET(sv_url, write_disk(sv_cache_file, overwrite = TRUE))\n  stop_for_status(sv_resp)\n}\n\nstreetview_img &lt;- readJPEG(sv_cache_file)\n\n\nsv_w &lt;- sv_size[1]\nsv_h &lt;- sv_size[2]\n\nsv_projected &lt;- cbind(\n  sun_path,\n  project_sun(sun_path$azimuth_deg, sun_path$altitude_deg,\n              sv_heading, sv_pitch, sv_fov, sv_fov, sv_w, sv_h)\n) |&gt;\n  filter(abs(delta_az) &lt;= sv_fov / 2, abs(delta_alt) &lt;= sv_fov / 2)\n\nsv_peak_projected &lt;- sv_projected |&gt; filter(date == peak_sun$date)\n\n\nggplot() +\n  annotation_raster(streetview_img, xmin = 0, xmax = sv_w, ymin = 0, ymax = sv_h) +\n  geom_path(data = sv_projected, aes(x = x, y = y), color = \"yellow\", linewidth = 1) +\n  geom_point(data = sv_peak_projected, aes(x = x, y = y), color = \"red\", size = 3) +\n  geom_text(data = sv_peak_projected, aes(x = x, y = y, label = time_bst),\n             color = \"red\", vjust = -1) +\n  coord_fixed(xlim = c(0, sv_w), ylim = c(0, sv_h), expand = FALSE) +\n  labs(\n    title = \"Sun's path during the 12 Aug 2026 partial solar eclipse\",\n    subtitle = paste0(\"Camera heading \", round(sv_heading, 1), \"°, pitch \",\n                       round(sv_pitch, 1), \"° — near Nottinghamshire/Lincolnshire border\"),\n    x = NULL, y = NULL\n  ) +\n  theme_void() +\n  theme(\n    plot.title = element_text(color = \"white\"),\n    plot.subtitle = element_text(color = \"white\"),\n    plot.background = element_rect(fill = \"black\")\n  )\n\n\n\n\n\n\n\nFigure 1: Sun’s path during the 12 Aug 2026 partial solar eclipse, projected onto a Street View image"
  },
  {
    "objectID": "eclipse_2026_sun_overlay.html#photo-overlay",
    "href": "eclipse_2026_sun_overlay.html#photo-overlay",
    "title": "Projecting the sun’s path for the 12 August 2026 UK partial solar eclipse",
    "section": "Photo overlay",
    "text": "Photo overlay\nThis photo was taken with the Theodolite app on an iPhone, which records GPS position and GPSImgDirection (true compass heading) in EXIF, and the camera’s vertical/horizontal inclination (vert_angle_deg / horiz_angle_deg) in the image caption. We treat vert_angle_deg as pitch and treat roll (horiz_angle_deg) as negligible.\n\nphoto_path &lt;- \"/Users/paul/Downloads/TH000003.JPG\"\n\nmeta &lt;- exif_read(photo_path)\n\nUsing ExifTool version 13.55\n\nphoto_lat &lt;- meta$GPSLatitude\nphoto_lon &lt;- meta$GPSLongitude\nheading   &lt;- meta$GPSImgDirection\npitch     &lt;- as.numeric(sub(\".*vert_angle_deg=([0-9.\\\\-]+).*\", \"\\\\1\", meta$ImageDescription))\n\nphoto_time_local &lt;- as.POSIXct(\n  paste(gsub(\":\", \"-\", substr(meta$DateTimeOriginal, 1, 10)), substr(meta$DateTimeOriginal, 12, 19)),\n  tz = \"UTC\"\n) - hours(as.numeric(substr(meta$OffsetTimeOriginal, 1, 3)))\n\nimg_w &lt;- meta$ImageWidth\nimg_h &lt;- meta$ImageHeight\n\nThe photo’s field of view is estimated from its 35mm-equivalent focal length, rescaled to the actual saved crop dimensions assuming the native ultra-wide sensor is 4032x3024 pixels (iPhone 13 Pro Max ultra-wide camera) and a rectilinear (tangent-plane) projection. This is an approximation — the lens’s true optical FOV may differ from this 35mm-equivalent estimate.\n\nnative_w &lt;- 4032\nf35 &lt;- meta$FocalLengthIn35mmFormat\nfov_h_native &lt;- 2 * atan(18 / f35) * 180 / pi\nfov_h &lt;- 2 * atan(tan(fov_h_native / 2 * pi / 180) * (img_w / native_w)) * 180 / pi\nfov_v &lt;- 2 * atan((img_h / img_w) * tan(fov_h / 2 * pi / 180)) * 180 / pi\n\nCamera heading 279.1°, pitch 2.1°, estimated field of view 87° x 48°, captured 14:08 UTC.\n\nsun_path_photo &lt;- getSunlightPosition(date = times, lat = photo_lat, lon = photo_lon) |&gt;\n  mutate(\n    azimuth_deg = (azimuth * 180 / pi + 180) %% 360,\n    altitude_deg = altitude * 180 / pi,\n    time_bst = format(with_tz(date, \"Europe/London\"), \"%H:%M\")\n  )\n\ncapture_sun &lt;- getSunlightPosition(date = photo_time_local, lat = photo_lat, lon = photo_lon) |&gt;\n  mutate(\n    azimuth_deg = (azimuth * 180 / pi + 180) %% 360,\n    altitude_deg = altitude * 180 / pi\n  )\n\n\nsun_projected &lt;- cbind(\n  sun_path_photo,\n  project_sun(sun_path_photo$azimuth_deg, sun_path_photo$altitude_deg,\n              heading, pitch, fov_h, fov_v, img_w, img_h)\n)\ncapture_projected &lt;- cbind(\n  capture_sun,\n  project_sun(capture_sun$azimuth_deg, capture_sun$altitude_deg,\n              heading, pitch, fov_h, fov_v, img_w, img_h)\n)\n\nin_frame &lt;- function(x, y) x &gt;= 0 & x &lt;= img_w & y &gt;= 0 & y &lt;= img_h\n\npath_in_frame &lt;- sun_projected |&gt; filter(in_frame(x, y))\ncapture_in_frame &lt;- in_frame(capture_projected$x[1], capture_projected$y[1])\n\npath_ticks &lt;- path_in_frame |&gt; filter(minute(date) %% 15 == 0)\n\nphoto_img &lt;- readJPEG(photo_path)\n\n\np_photo &lt;- ggplot() +\n  annotation_raster(photo_img, xmin = 0, xmax = img_w, ymin = 0, ymax = img_h) +\n  coord_fixed(xlim = c(0, img_w), ylim = c(0, img_h), expand = FALSE) +\n  theme_void() +\n  labs(\n    title = \"Eclipse sun path projected onto photo\",\n    subtitle = paste0(\n      \"Heading \", round(heading, 1), \"°, pitch \", round(pitch, 1),\n      \"°, est. FOV \", round(fov_h, 0), \"°x\", round(fov_v, 0),\n      \"°, captured \", format(photo_time_local, \"%H:%M UTC\")\n    ),\n    x = NULL, y = NULL\n  ) +\n  theme(\n    plot.title = element_text(color = \"white\", size = 11),\n    plot.subtitle = element_text(color = \"white\", size = 9),\n    plot.background = element_rect(fill = \"black\")\n  )\n\nif (nrow(path_in_frame) &gt; 0) {\n  p_photo &lt;- p_photo +\n    geom_path(data = path_in_frame, aes(x = x, y = y), color = \"yellow\", linewidth = 1) +\n    geom_point(data = path_ticks, aes(x = x, y = y), color = \"yellow\", size = 2) +\n    geom_label(\n      data = path_ticks, aes(x = x, y = y, label = time_bst),\n      color = \"black\", fill = \"yellow\", alpha = 0.8, size = 2.8,\n      linewidth = 0, label.padding = unit(0.12, \"lines\"), vjust = -0.5\n    )\n}\n\nif (isTRUE(capture_in_frame)) {\n  p_photo &lt;- p_photo +\n    geom_point(data = capture_projected, aes(x = x, y = y), color = \"red\", size = 3) +\n    geom_label(data = capture_projected, aes(x = x, y = y, label = \"sun at capture\"),\n               color = \"red\", fill = \"white\", alpha = 0.8, linewidth = 0, vjust = -0.7)\n} else {\n  cx &lt;- pmin(pmax(capture_projected$x, 0), img_w)\n  cy &lt;- pmin(pmax(capture_projected$y, 0), img_h)\n  offframe_label &lt;- sprintf(\n    \"Sun at capture (%s) is %.0f° off-frame\\n(Az %.0f°, Alt %.0f° vs heading %.0f°, pitch %.0f°)\",\n    format(photo_time_local, \"%H:%M UTC\"),\n    sqrt(capture_projected$delta_az^2 + capture_projected$delta_alt^2),\n    capture_sun$azimuth_deg, capture_sun$altitude_deg, heading, pitch\n  )\n  p_photo &lt;- p_photo +\n    annotate(\"segment\", x = img_w / 2, y = img_h / 2, xend = cx, yend = cy,\n             arrow = arrow(length = unit(0.3, \"cm\")), color = \"red\", linewidth = 1) +\n    annotate(\"label\", x = img_w * 0.02, y = img_h * 0.02, label = offframe_label,\n             color = \"red\", fill = \"white\", alpha = 0.85, size = 3, hjust = 0, vjust = 0,\n             linewidth = 0)\n}\n\nif (nrow(path_in_frame) == 0) {\n  p_photo &lt;- p_photo +\n    annotate(\"label\", x = img_w * 0.02, y = img_h * 0.98,\n             label = \"Eclipse sun path (16:30-19:15 UTC) never enters this frame\",\n             color = \"black\", fill = \"yellow\", alpha = 0.85, size = 3, hjust = 0, vjust = 1,\n             linewidth = 0)\n}\n\np_photo\n\n\n\n\n\n\n\nFigure 2: Eclipse sun path projected onto the Theodolite photo"
  },
  {
    "objectID": "eclipse_2026_sun_overlay.html#terrain-and-named-peaks-within-10-miles",
    "href": "eclipse_2026_sun_overlay.html#terrain-and-named-peaks-within-10-miles",
    "title": "Projecting the sun’s path for the 12 August 2026 UK partial solar eclipse",
    "section": "Terrain and named peaks within 10 miles",
    "text": "Terrain and named peaks within 10 miles\nTo give context on nearby high ground that could obstruct a low-altitude view of the eclipse, this section builds a 3D terrain surface for a 10-mile radius around the viewing location and labels the named hills found in OpenStreetMap within that area.\n\nmiles_to_deg_lat &lt;- 10 / 69.0\nmiles_to_deg_lon &lt;- 10 / (69.0 * cos(lat * pi / 180))\n\nbbox_sf &lt;- st_as_sf(\n  data.frame(\n    lon = c(lon - miles_to_deg_lon, lon + miles_to_deg_lon),\n    lat = c(lat - miles_to_deg_lat, lat + miles_to_deg_lat)\n  ),\n  coords = c(\"lon\", \"lat\"), crs = 4326\n)\n\n# Cache as a GeoTIFF (rather than saveRDS) so the raster's values are\n# guaranteed to round-trip regardless of in-memory vs. file-backed state.\nelev_cache_file &lt;- file.path(\n  cache_dir, sprintf(\"elev_raster_%s_%s_z10.tif\", lat, lon)\n)\n\nif (file.exists(elev_cache_file)) {\n  elev_raster &lt;- raster::raster(elev_cache_file)\n} else {\n  elev_raster &lt;- get_elev_raster(bbox_sf, z = 10, src = \"aws\")\n  raster::writeRaster(elev_raster, elev_cache_file, overwrite = TRUE)\n}\n\n# Reproject to British National Grid so grid cells are isotropic (equal\n# distance in x and y), which raw lat/lon degrees are not at this latitude\nr_bng &lt;- terra::project(terra::rast(elev_raster), \"EPSG:27700\", method = \"bilinear\", res = 30)\n\n\n# osmdata's default Overpass mirror can vary and has been observed rate-\n# limiting/blocking repeated requests from this machine (surfacing as a\n# 429/420-style error after a long retry backoff). Pin to the main\n# overpass-api.de instance, which has been confirmed reachable.\nset_overpass_url(\"https://overpass-api.de/api/interpreter\")\n\nbbox_num &lt;- c(lon - miles_to_deg_lon, lat - miles_to_deg_lat, lon + miles_to_deg_lon, lat + miles_to_deg_lat)\n\npeaks_cache_file &lt;- file.path(cache_dir, sprintf(\"osm_peaks_%s_%s.rds\", lat, lon))\n\nif (file.exists(peaks_cache_file)) {\n  peaks &lt;- readRDS(peaks_cache_file)\n} else {\n  peaks &lt;- opq(bbox = bbox_num, timeout = 120) |&gt;\n    add_osm_feature(key = \"natural\", value = \"peak\") |&gt;\n    osmdata_sf()\n  saveRDS(peaks, peaks_cache_file)\n}\n\npeak_named &lt;- peaks$osm_points |&gt; filter(!is.na(name))\n\npeak_named_bng &lt;- st_transform(peak_named, 27700)\npeak_coords &lt;- st_coordinates(peak_named_bng)\n\npeak_df &lt;- data.frame(\n  name = peak_named_bng$name,\n  easting = peak_coords[, \"X\"],\n  northing = peak_coords[, \"Y\"]\n) |&gt;\n  distinct(name, .keep_all = TRUE)\n\n# Look up each peak's elevation from the DEM rather than relying on\n# (frequently missing) OSM elevation tags\npeak_df$elev_dem &lt;- terra::extract(r_bng, as.matrix(peak_df[, c(\"easting\", \"northing\")]))[, 1]\npeak_df &lt;- peak_df |&gt; arrange(desc(elev_dem))\n\ntop_peaks &lt;- head(peak_df, 8)\n\n# The eclipse-viewing location itself (Long Eaton), for reference on the map\nviewing_point_bng &lt;- st_transform(st_sfc(st_point(c(lon, lat)), crs = 4326), 27700)\nviewing_coords &lt;- st_coordinates(viewing_point_bng)\nviewing_point &lt;- data.frame(\n  name = \"Long Eaton\",\n  easting = viewing_coords[, \"X\"],\n  northing = viewing_coords[, \"Y\"]\n)\nviewing_point$elev_dem &lt;- terra::extract(r_bng, viewing_coords)[, 1]\n\n# The DEM's highest point can sit outside any *named* OSM peak - here it's\n# ~25km from the nearest tagged peak (Drum Hill), so it's real high ground\n# (likely Peak District/Pennine foothills at the edge of the 10-mile-radius\n# bounding box, whose corners reach ~14 miles out) rather than an OSM gap\ndem_vals &lt;- terra::values(r_bng)[, 1]\ndem_xy &lt;- terra::xyFromCell(r_bng, seq_along(dem_vals))\nmax_idx &lt;- which.max(dem_vals)\nhigh_point &lt;- data.frame(\n  name = \"Unnamed high ground\",\n  easting = dem_xy[max_idx, 1],\n  northing = dem_xy[max_idx, 2],\n  elev_dem = dem_vals[max_idx]\n)\n\nWithin this radius, 15 named hills are tagged in OpenStreetMap, ranging from 27 m to 186 m; the eight highest are labeled below, along with the eclipse-viewing location (Long Eaton, at the centre) and the single highest point in the DEM overall (381 m), which falls outside the named-peaks dataset and sits at the edge of the frame.\n\n# NOTE: raster_to_matrix() transposes the raster - its first matrix\n# dimension indexes eastings (raster columns), its second indexes\n# northings (raster rows, north-to-south)\nelmat &lt;- raster_to_matrix(r_bng)\n\next_bng &lt;- terra::ext(r_bng)\neastings_full &lt;- seq(ext_bng[1], ext_bng[2], length.out = ncol(r_bng))\nnorthings_full &lt;- seq(ext_bng[4], ext_bng[3], length.out = nrow(r_bng))\n\n# Downsample for a responsive interactive plot\nstep &lt;- 3\ni_seq &lt;- seq(1, nrow(elmat), by = step)\nj_seq &lt;- seq(1, ncol(elmat), by = step)\nelmat_ds &lt;- elmat[i_seq, j_seq]\neastings_ds &lt;- eastings_full[i_seq]\nnorthings_ds &lt;- northings_full[j_seq]\n\n# plotly's z matrix rows -&gt; y axis, cols -&gt; x axis\nz_mat &lt;- t(elmat_ds)\n\n\npeak_hover &lt;- sprintf(\"%s&lt;br&gt;%d m\", top_peaks$name, round(top_peaks$elev_dem))\nviewing_hover &lt;- sprintf(\"%s&lt;br&gt;%d m\", viewing_point$name, round(viewing_point$elev_dem))\nhigh_point_hover &lt;- sprintf(\"%s&lt;br&gt;%d m\", high_point$name, round(high_point$elev_dem))\n\nplot_ly() |&gt;\n  add_surface(x = eastings_ds, y = northings_ds, z = z_mat,\n              colorscale = \"Earth\", showscale = FALSE) |&gt;\n  add_trace(\n    type = \"scatter3d\", mode = \"markers+text\",\n    x = top_peaks$easting, y = top_peaks$northing, z = top_peaks$elev_dem + 15,\n    text = top_peaks$name, textposition = \"top center\",\n    marker = list(color = \"red\", size = 4),\n    textfont = list(color = \"black\", size = 11),\n    hovertext = peak_hover, hoverinfo = \"text\",\n    showlegend = FALSE\n  ) |&gt;\n  add_trace(\n    type = \"scatter3d\", mode = \"markers+text\",\n    x = viewing_point$easting, y = viewing_point$northing, z = viewing_point$elev_dem + 15,\n    text = viewing_point$name, textposition = \"top center\",\n    marker = list(color = \"blue\", size = 5, symbol = \"diamond\"),\n    textfont = list(color = \"black\", size = 11),\n    hovertext = viewing_hover, hoverinfo = \"text\",\n    showlegend = FALSE\n  ) |&gt;\n  add_trace(\n    type = \"scatter3d\", mode = \"markers+text\",\n    x = high_point$easting, y = high_point$northing, z = high_point$elev_dem + 15,\n    text = high_point$name, textposition = \"top center\",\n    marker = list(color = \"darkorange\", size = 4),\n    textfont = list(color = \"black\", size = 11),\n    hovertext = high_point_hover, hoverinfo = \"text\",\n    showlegend = FALSE\n  ) |&gt;\n  layout(\n    title = \"Terrain within 10 miles of the eclipse-viewing location\",\n    scene = list(\n      xaxis = list(title = \"Easting (m, BNG)\"),\n      yaxis = list(title = \"Northing (m, BNG)\"),\n      zaxis = list(title = \"Elevation (m)\"),\n      aspectmode = \"manual\",\n      aspectratio = list(x = 1, y = diff(range(northings_ds)) / diff(range(eastings_ds)), z = 0.25)\n    )\n  )\n\n\n\n\n\n\n\nFigure 3: Terrain within 10 miles of the eclipse-viewing location, with the highest named hills labeled"
  },
  {
    "objectID": "eclipse_2026_sun_overlay.html#river-network-and-3d-rendering",
    "href": "eclipse_2026_sun_overlay.html#river-network-and-3d-rendering",
    "title": "Projecting the sun’s path for the 12 August 2026 UK partial solar eclipse",
    "section": "River network and 3D rendering",
    "text": "River network and 3D rendering\nTo add geographic context beyond bare terrain, the local river/canal network is draped over a full-resolution rayshader hillshade and rendered as a 3D scene. Watercourse geometry comes from Ordnance Survey’s OS Open Rivers dataset rather than OpenStreetMap, since public Overpass API mirrors proved unreliable (repeated 504 timeouts) when queried for waterway features.\n\n# OS Open Rivers (OGL/Crown copyright, free via OS Data Hub) - downloaded once\n# and cached, since Overpass mirrors were unreliable for `waterway` queries.\nrivers_zip &lt;- file.path(cache_dir, \"os_open_rivers_gb.gpkg\")\nrivers_extract_dir &lt;- file.path(cache_dir, \"os_open_rivers_extracted\")\nrivers_gpkg &lt;- file.path(rivers_extract_dir, \"Data\", \"oprvrs_gb.gpkg\")\n\nif (!file.exists(rivers_gpkg)) {\n  if (!file.exists(rivers_zip)) {\n    # download.file()/libcurl can trip sandboxes that block reading ~/.netrc;\n    # disabling netrc lookup on the curl handle avoids that\n    h &lt;- curl::new_handle(netrc = 0, followlocation = TRUE, timeout = 600)\n    curl::curl_download(\n      \"https://api.os.uk/downloads/v1/products/OpenRivers/downloads?area=GB&format=GeoPackage&redirect\",\n      destfile = rivers_zip, handle = h\n    )\n  }\n  # The download is actually a zip containing the real .gpkg plus licence/readme docs\n  unzip(rivers_zip, exdir = rivers_extract_dir)\n}\n\n\nbbox_bng &lt;- st_bbox(st_transform(bbox_sf, 27700))\n\nrivers_bng &lt;- st_read(\n  rivers_gpkg, layer = \"watercourse_link\",\n  wkt_filter = st_as_text(st_as_sfc(bbox_bng)),\n  quiet = TRUE\n)\n\nWithin the same 10-mile radius, 590 watercourse segments are found, including named rivers and canals such as River Trent.\n\n# Full-resolution shading (not the downsampled elmat_ds used for the plotly\n# surface), since this feeds the 3D rayshader render below\nhillshade &lt;- elmat |&gt;\n  sphere_shade(texture = \"imhof1\") |&gt;\n  add_shadow(ray_shade(elmat, zscale = 30, multicore = TRUE), max_darken = 0.5) |&gt;\n  add_shadow(ambient_shade(elmat, zscale = 30), max_darken = 0.5)\n\nriver_overlay &lt;- generate_line_overlay(\n  rivers_bng, extent = ext_bng, heightmap = elmat,\n  color = \"steelblue4\", linewidth = 3\n)\n\nhillshade_rivers &lt;- add_overlay(hillshade, river_overlay)\n\n\nsnapshot_file &lt;- file.path(cache_dir, \"terrain_3d_rivers_snapshot.png\")\n\nrgl::close3d()\n# zscale = 30 (cell size) / 5 exaggeration; at true scale (zscale = 30) or\n# even 2x (zscale = 15) the ~415 m relief across a 30 km extent reads as\n# nearly flat, so a stronger exaggeration and lower, more oblique phi make\n# the terrain (and the rivers following its valleys) legible\nplot_3d(\n  hillshade_rivers, elmat,\n  zscale = 6, windowsize = c(1000, 800),\n  phi = 25, theta = 45, zoom = 0.6,\n  solid = FALSE, shadowdepth = -50\n)\n\nzscale_3d &lt;- 6\n\nfor (i in seq_len(nrow(top_peaks))) {\n  # altitude is relative to the *local* terrain height (relativez = TRUE) and\n  # gets stretched by zscale, so a modest +40 m keeps labels near the surface\n  render_label(\n    elmat, extent = ext_bng, zscale = zscale_3d,\n    lat = top_peaks$northing[i], long = top_peaks$easting[i],\n    altitude = 40, offset = 0,\n    text = top_peaks$name[i], textsize = 1.1, linewidth = 2,\n    clear_previous = (i == 1)\n  )\n}\n\nrender_label(\n  elmat, extent = ext_bng, zscale = zscale_3d,\n  lat = viewing_point$northing, long = viewing_point$easting,\n  altitude = 50, offset = 0,\n  text = viewing_point$name, textsize = 1.3, linewidth = 2,\n  textcolor = \"darkblue\", linecolor = \"darkblue\"\n)\n\nrender_label(\n  elmat, extent = ext_bng, zscale = zscale_3d,\n  lat = high_point$northing, long = high_point$easting,\n  altitude = 40, offset = 0,\n  text = sprintf(\"%s (%d m)\", high_point$name, round(high_point$elev_dem)),\n  textsize = 1.1, linewidth = 2,\n  textcolor = \"darkorange4\", linecolor = \"darkorange4\"\n)\n\nSys.sleep(3)\nrender_snapshot(filename = snapshot_file)\nrgl::close3d()\n\nknitr::include_graphics(snapshot_file)\n\n\n\n\n\n\n\nFigure 4: 3D terrain rendering (5x vertical exaggeration) with the river network, named peaks, the eclipse-viewing location, and the highest (unnamed) point in the frame labeled"
  },
  {
    "objectID": "eclipse_2026_sun_overlay.html#notes-and-caveats",
    "href": "eclipse_2026_sun_overlay.html#notes-and-caveats",
    "title": "Projecting the sun’s path for the 12 August 2026 UK partial solar eclipse",
    "section": "Notes and caveats",
    "text": "Notes and caveats\n\nBoth photo overlays use a small-angle/linear projection from angular offset to pixel offset around the camera’s heading and pitch, rather than a true perspective (gnomonic) projection. This is a reasonable approximation for the field-of-view sizes and angular ranges involved here, but introduces increasing distortion toward the edges of wide fields of view.\nThe Street View camera’s field of view is assumed to be 90° both horizontally and vertically (the API default for a square image); the true vertical field of view for the Street View panorama projection may differ.\nThe photo’s field of view is estimated from its 35mm-equivalent focal length and an assumed native sensor resolution (4032x3024 for the iPhone 13 Pro Max ultra-wide camera), rescaled to the actual saved crop size. This is not a verified optical specification.\nThe photo’s pitch is taken directly from the Theodolite app’s vert_angle_deg caption field, and roll (horiz_angle_deg) is assumed negligible and not corrected for.\nThe terrain elevation data comes from AWS Terrain Tiles (~30m resolution) via elevatr. Named peaks are whichever natural=peak features happen to be tagged in OpenStreetMap within the radius — this list may be incomplete, and their elevations are read from the DEM at each point’s coordinates rather than from (often-missing) OSM elevation tags.\nThe river network is watercourse-link geometry from OS Open Rivers. Contains OS data © Crown copyright and database right, available under the Open Government Licence. It is a generalised network, not a detailed hydrography product, and roughly 200 of the segments in this radius carry no watercourse_name (minor streams/ditches).\nReprojecting the elevation raster from WGS84 to British National Grid leaves a small fraction (~2%) of edge cells as NA, visible as a faint ragged border in the 3D render.\nThe bounding box is built as a square in degrees (±10 miles in latitude and longitude), so its corners reach roughly 14 miles from the viewing location — farther than the stated 10-mile radius. The highest point in the frame (~381 m) falls in one such corner and is ~25 km from the nearest named OSM peak, so it is almost certainly real high ground (plausibly Peak District/Pennine foothills) rather than a labeling gap — OSM just has no natural=peak point there."
  },
  {
    "objectID": "explainers/markov-chains-diffusion/index.html",
    "href": "explainers/markov-chains-diffusion/index.html",
    "title": "Markov Chains, Concretely",
    "section": "",
    "text": "This is the first of three short refreshers behind two earlier posts — Reproducing a Phase Transition in Diffusion Models and Fixed Points, Phase Transitions, and Diffusion Models — both of which lean on ideas from Sclocchi, Favero & Wyart (2024) (Sclocchi, Favero, and Wyart 2024) that are easy to gloss over if you haven’t seen them recently. This one covers Markov chains; the next two cover mean-field analysis and the generative diffusion process itself."
  },
  {
    "objectID": "explainers/markov-chains-diffusion/index.html#what-makes-a-process-markov",
    "href": "explainers/markov-chains-diffusion/index.html#what-makes-a-process-markov",
    "title": "Markov Chains, Concretely",
    "section": "What makes a process “Markov”",
    "text": "What makes a process “Markov”\nA process is Markov if its next state depends only on its current state — not on the path that got it there. Formally, \\(P(x_{t+1} \\mid x_t, x_{t-1}, \\dots, x_0) = P(x_{t+1} \\mid x_t)\\). That’s it; the entire history collapses into whatever’s true right now.\nA simple weather model makes this concrete: three states, and a transition matrix giving the probability of tomorrow’s weather given today’s.\n\nstates &lt;- c(\"Sunny\", \"Cloudy\", \"Rainy\")\nP &lt;- matrix(\n  c(0.7, 0.2, 0.1,\n    0.3, 0.4, 0.3,\n    0.2, 0.3, 0.5),\n  nrow = 3, byrow = TRUE, dimnames = list(states, states)\n)\n\nset.seed(1)\nn_steps &lt;- 200\npath &lt;- integer(n_steps)\npath[1] &lt;- 1L\nfor (t in 2:n_steps) path[t] &lt;- sample.int(3, 1, prob = P[path[t - 1], ])\n\ndata.frame(step = seq_len(n_steps), state = factor(states[path], levels = states)) |&gt;\n  ggplot(aes(step, state, colour = state)) +\n  geom_point(size = 1.5) +\n  labs(x = \"step\", y = NULL, colour = NULL, title = \"Simulated weather path\") +\n  theme(legend.position = \"top\")\n\n\n\n\n\n\n\nFigure 1: A 200-step simulated path through the weather chain, colour-coded by state.\n\n\n\n\n\nKnowing today is Sunny tells you everything relevant about tomorrow; knowing it was also Sunny for the last five days adds nothing. That’s the whole idea, and it’s what makes chains like this tractable to analyze — no exploding state space of “all possible histories” to track."
  },
  {
    "objectID": "explainers/markov-chains-diffusion/index.html#where-does-it-settle-two-ways-to-find-out",
    "href": "explainers/markov-chains-diffusion/index.html#where-does-it-settle-two-ways-to-find-out",
    "title": "Markov Chains, Concretely",
    "section": "Where does it settle? Two ways to find out",
    "text": "Where does it settle? Two ways to find out\nRun the chain long enough and the fraction of time spent in each state converges to a fixed stationary distribution \\(\\pi\\), satisfying \\(\\pi P = \\pi\\) — a left eigenvector of \\(P\\) with eigenvalue 1. Two independent ways to find it should agree:\n\neig &lt;- eigen(t(P))\npi_stat &lt;- Re(eig$vectors[, which.min(abs(eig$values - 1))])\npi_stat &lt;- pi_stat / sum(pi_stat)\nnames(pi_stat) &lt;- states\n\nn_long &lt;- 5000\npath_long &lt;- integer(n_long)\npath_long[1] &lt;- 1L\nfor (t in 2:n_long) path_long[t] &lt;- sample.int(3, 1, prob = P[path_long[t - 1], ])\npi_empirical &lt;- table(factor(states[path_long], levels = states)) / n_long\n\ndf &lt;- data.frame(\n  state = rep(states, 2),\n  probability = c(pi_stat, as.numeric(pi_empirical)),\n  method = rep(c(\"eigenvector\", \"5000-step simulation\"), each = 3)\n)\n\nggplot(df, aes(state, probability, fill = method)) +\n  geom_col(position = \"dodge\") +\n  labs(x = NULL, y = \"probability\", fill = NULL)\n\n\n\n\n\n\n\nFigure 2: Stationary distribution via eigen-decomposition vs. a long simulated path’s empirical frequencies.\n\n\n\n\n\nEigenvector: Sunny 0.457, Cloudy 0.283, Rainy 0.261. A long simulated path lands close to the same numbers — the chain has “forgotten” where it started and settled into its long-run behaviour. This idea — a distribution that’s a fixed point of an update rule — will come back in the next post, where a self-consistency equation plays exactly the same role."
  },
  {
    "objectID": "explainers/markov-chains-diffusion/index.html#ddpms-actual-markov-chain",
    "href": "explainers/markov-chains-diffusion/index.html#ddpms-actual-markov-chain",
    "title": "Markov Chains, Concretely",
    "section": "DDPM’s actual Markov chain",
    "text": "DDPM’s actual Markov chain\nDiffusion models run a Markov chain too, just with a continuous state and Gaussian transition kernels rather than a lookup table. The forward (noising) process is defined, exactly as in Ho, Jain & Abbeel (2020) (Ho, Jain, and Abbeel 2020), by:\n\\[\nq(x_t \\mid x_{t-1}) = \\mathcal{N}\\!\\left(\\sqrt{1-\\beta_t}\\, x_{t-1},\\ \\beta_t I\\right)\n\\]\nEach step nudges \\(x_{t-1}\\) slightly toward the origin and adds a little Gaussian noise, with \\(\\beta_t\\) controlling how much. This is precisely the recurrence used in guided_diffusion/gaussian_diffusion.py, the codebase reproduced in the diffusion-phase-transition post.\n\nT_steps &lt;- 1000\nbeta &lt;- seq(1e-4, 0.02, length.out = T_steps)\nalpha &lt;- 1 - beta\nalpha_bar &lt;- cumprod(alpha)\n\nx0 &lt;- 2.0\nn_traj &lt;- 8\nset.seed(2)\ntraj &lt;- matrix(NA_real_, T_steps + 1, n_traj)\ntraj[1, ] &lt;- x0\nfor (t in 1:T_steps) {\n  eps &lt;- rnorm(n_traj)\n  traj[t + 1, ] &lt;- sqrt(alpha[t]) * traj[t, ] + sqrt(beta[t]) * eps\n}\n\ntraj_df &lt;- as.data.frame(traj) |&gt;\n  mutate(step = 0:T_steps) |&gt;\n  tidyr::pivot_longer(-step, names_to = \"trajectory\", values_to = \"x\")\n\nband_df &lt;- data.frame(step = 0:T_steps, sd = sqrt(1 - alpha_bar[c(1, seq_len(T_steps))]))\n\nggplot() +\n  geom_ribbon(data = band_df, aes(step, ymin = -2 * sd, ymax = 2 * sd), fill = \"grey85\") +\n  geom_ribbon(data = band_df, aes(step, ymin = -sd, ymax = sd), fill = \"grey70\") +\n  geom_line(data = traj_df, aes(step, x, group = trajectory), colour = \"steelblue\", linewidth = 0.4) +\n  labs(x = \"t\", y = expression(x[t]), title = \"Forward chain trajectories vs. their theoretical envelope\")\n\n\n\n\n\n\n\nFigure 3: Eight sample trajectories of the scalar Gaussian chain, starting from x0=2, collapsing into the N(0,1) envelope (shaded bands at +-1, +-2 sd) under the real Ho et al. (2020) schedule."
  },
  {
    "objectID": "explainers/markov-chains-diffusion/index.html#the-closed-form-jump",
    "href": "explainers/markov-chains-diffusion/index.html#the-closed-form-jump",
    "title": "Markov Chains, Concretely",
    "section": "The closed-form jump",
    "text": "The closed-form jump\nSimulating 1000 sequential steps every time you need a noisy sample would be wasteful, and because each step’s kernel is Gaussian, it’s also unnecessary: the composition of \\(t\\) Gaussian steps is itself Gaussian, in closed form:\n\\[\nq(x_t \\mid x_0) = \\mathcal{N}\\!\\left(\\sqrt{\\bar\\alpha_t}\\, x_0,\\ (1-\\bar\\alpha_t) I\\right), \\qquad \\bar\\alpha_t = \\prod_{s \\le t}(1-\\beta_s)\n\\]\nYou can jump straight from \\(x_0\\) to any \\(x_t\\) in one draw. This is exactly q_sample in the reproduced code, and it’s what makes the “forward-diffuse-partway-then-reverse” experiment in the diffusion-phase-transition post practical on real images. Worth actually checking it’s true rather than taking it on faith:\n\ncheck_t &lt;- 30\nn_mc &lt;- 200000\nset.seed(3)\nx_stepwise &lt;- rep(x0, n_mc)\nfor (t in 1:check_t) {\n  eps &lt;- rnorm(n_mc)\n  x_stepwise &lt;- sqrt(alpha[t]) * x_stepwise + sqrt(beta[t]) * eps\n}\nmean_cf &lt;- sqrt(alpha_bar[check_t]) * x0\nvar_cf &lt;- 1 - alpha_bar[check_t]\n\nAt \\(t=30\\), 200,000 step-by-step Monte Carlo draws give mean 1.9885 and variance 0.01163; the closed form predicts mean 1.9884 and variance 0.0116 — matching to within Monte Carlo noise."
  },
  {
    "objectID": "explainers/markov-chains-diffusion/index.html#the-reverse-chain",
    "href": "explainers/markov-chains-diffusion/index.html#the-reverse-chain",
    "title": "Markov Chains, Concretely",
    "section": "The reverse chain",
    "text": "The reverse chain\nThe reverse process \\(p_\\theta(x_{t-1} \\mid x_t)\\) is modelled the same way — Markov, approximately Gaussian for small \\(\\beta_t\\) — but its mean isn’t known in closed form for real data; that’s what a neural network is trained to approximate. The next explainer is a detour into a different piece of machinery this paper needs (mean-field analysis); the third comes back to build a toy reverse chain where, unusually, the right answer is known exactly."
  },
  {
    "objectID": "explainers/markov-chains-diffusion/index.html#references",
    "href": "explainers/markov-chains-diffusion/index.html#references",
    "title": "Markov Chains, Concretely",
    "section": "References",
    "text": "References\n\n\nHo, Jonathan, Ajay Jain, and Pieter Abbeel. 2020. “Denoising Diffusion Probabilistic Models.” arXiv Preprint arXiv:2006.11239.\n\n\nSclocchi, Antonio, Alessandro Favero, and Matthieu Wyart. 2024. “A Phase Transition in Diffusion Models Reveals the Hierarchical Nature of Data.” arXiv Preprint arXiv:2402.16991."
  },
  {
    "objectID": "explainers/toy-diffusion-model/index.html",
    "href": "explainers/toy-diffusion-model/index.html",
    "title": "A Diffusion Model You Can Actually Solve",
    "section": "",
    "text": "Third and final part of a series behind Reproducing a Phase Transition in Diffusion Models and Fixed Points, Phase Transitions, and Diffusion Models. This one assumes the first post’s closed-form jump \\(q(x_t\\mid x_0)\\); the second is a self-contained detour and isn’t needed here."
  },
  {
    "objectID": "explainers/toy-diffusion-model/index.html#noising-a-whole-distribution",
    "href": "explainers/toy-diffusion-model/index.html#noising-a-whole-distribution",
    "title": "A Diffusion Model You Can Actually Solve",
    "section": "Noising a whole distribution",
    "text": "Noising a whole distribution\nThe first post applied the forward chain to a single starting point, \\(x_0=2\\). Real data isn’t a point, it’s a distribution — so apply the same closed-form jump to a batch of samples instead. Take something with actual structure to destroy: a two-component mixture, two bumps at \\(-2\\) and \\(+2\\).\n\nmu1 &lt;- -2; mu2 &lt;- 2; sd0 &lt;- 0.4; w1 &lt;- 0.5; w2 &lt;- 0.5\nr_data &lt;- function(n) {\n  is_mode2 &lt;- rbinom(n, 1, w2)\n  ifelse(is_mode2 == 1, rnorm(n, mu2, sd0), rnorm(n, mu1, sd0))\n}\n\nset.seed(7)\nn &lt;- 4000\nx0_data &lt;- r_data(n)\n\nT_steps &lt;- 400\nbeta &lt;- seq(1e-4, 0.02, length.out = T_steps)\nalpha &lt;- 1 - beta\nalpha_bar &lt;- cumprod(alpha)\n\nnoise_to_t &lt;- function(x0_vec, t) {\n  sqrt(alpha_bar[t]) * x0_vec + sqrt(1 - alpha_bar[t]) * rnorm(length(x0_vec))\n}\nx_mid &lt;- noise_to_t(x0_data, T_steps %/% 2)\nx_T &lt;- noise_to_t(x0_data, T_steps)\n\ndata.frame(\n  x = c(x0_data, x_mid, x_T),\n  t = factor(rep(c(\"t = 0\", paste0(\"t = \", T_steps %/% 2), paste0(\"t = \", T_steps)),\n                 each = n),\n             levels = c(\"t = 0\", paste0(\"t = \", T_steps %/% 2), paste0(\"t = \", T_steps)))\n) |&gt;\n  ggplot(aes(x, fill = t)) +\n  geom_histogram(aes(y = after_stat(density)), bins = 60, alpha = 0.7, position = \"identity\") +\n  facet_wrap(~t, ncol = 1) +\n  labs(y = \"density\", fill = NULL) +\n  theme(legend.position = \"none\")\n\n\n\n\n\n\n\nFigure 1: The bimodal starting distribution collapsing to N(0,1) as t increases, under the same schedule shape as Ho et al. (2020).\n\n\n\n\n\nBy \\(t=400\\), mean \\(-0.0108\\) and sd \\(1.0414\\) — the two bumps have merged into plain \\(\\mathcal{N}(0,1)\\), no trace of where they started."
  },
  {
    "objectID": "explainers/toy-diffusion-model/index.html#what-a-denoiser-actually-needs-the-score",
    "href": "explainers/toy-diffusion-model/index.html#what-a-denoiser-actually-needs-the-score",
    "title": "A Diffusion Model You Can Actually Solve",
    "section": "What a denoiser actually needs: the score",
    "text": "What a denoiser actually needs: the score\nReversing this requires knowing which direction “more like the data” is, at every noise level. That direction is the score, \\(\\nabla_x \\log p_t(x)\\) — the gradient of the log-density of the noisy marginal at time \\(t\\). Tweedie’s formula relates it directly to the optimal denoiser: the best single-step estimate of the clean signal given a noisy \\(x_t\\) is a simple function of the score. This is the modern way to think about what a diffusion model’s network is trained to produce — not “the clean image” directly, but a time-indexed family of score estimates, one per noise level."
  },
  {
    "objectID": "explainers/toy-diffusion-model/index.html#the-one-case-you-dont-need-a-network-for",
    "href": "explainers/toy-diffusion-model/index.html#the-one-case-you-dont-need-a-network-for",
    "title": "A Diffusion Model You Can Actually Solve",
    "section": "The one case you don’t need a network for",
    "text": "The one case you don’t need a network for\nFor almost any real data distribution, the score has no closed form and has to be learned. But a Gaussian mixture is a rare exception: convolving a Gaussian mixture with Gaussian noise gives another Gaussian mixture, so the noised marginal at every \\(t\\) — and hence its score — stays exactly computable:\n\\[\np_t(x) = \\sum_k w_k\\, \\mathcal{N}\\!\\left(x;\\ \\sqrt{\\bar\\alpha_t}\\,\\mu_k,\\ \\ \\bar\\alpha_t \\sigma_0^2 + (1-\\bar\\alpha_t)\\right)\n\\]\n\nexact_score &lt;- function(x, t) {\n  abar &lt;- alpha_bar[t]\n  v &lt;- abar * sd0^2 + (1 - abar)\n  m1 &lt;- sqrt(abar) * mu1\n  m2 &lt;- sqrt(abar) * mu2\n  d1 &lt;- dnorm(x, m1, sqrt(v))\n  d2 &lt;- dnorm(x, m2, sqrt(v))\n  p &lt;- w1 * d1 + w2 * d2\n  (w1 * d1 * (-(x - m1) / v) + w2 * d2 * (-(x - m2) / v)) / p\n}\n\nlog_p_t &lt;- function(x, t) {\n  abar &lt;- alpha_bar[t]\n  v &lt;- abar * sd0^2 + (1 - abar)\n  m1 &lt;- sqrt(abar) * mu1\n  m2 &lt;- sqrt(abar) * mu2\n  log(w1 * dnorm(x, m1, sqrt(v)) + w2 * dnorm(x, m2, sqrt(v)))\n}\n\nh &lt;- 1e-5\ntest_x &lt;- c(-1, 0, 0.5, 1.5)\nnumeric_score &lt;- (log_p_t(test_x + h, 100) - log_p_t(test_x - h, 100)) / (2 * h)\nanalytic_score &lt;- exact_score(test_x, 100)\n\nChecked against a numerical derivative of \\(\\log p_t(x)\\) at \\(t=100\\), four test points: numeric gives -2.16329, 0, 3.52389, 0.7375, the closed form gives -2.16329, 0, 3.52389, 0.7375 — matching to the precision shown."
  },
  {
    "objectID": "explainers/toy-diffusion-model/index.html#reversing-it-exactly",
    "href": "explainers/toy-diffusion-model/index.html#reversing-it-exactly",
    "title": "A Diffusion Model You Can Actually Solve",
    "section": "Reversing it, exactly",
    "text": "Reversing it, exactly\nThe ancestral-sampling reverse update, written in terms of the score:\n\\[\nx_{t-1} = \\frac{1}{\\sqrt{\\alpha_t}}\\Big(x_t + \\beta_t\\, \\nabla_x \\log p_t(x_t)\\Big) + \\sqrt{\\beta_t}\\, z, \\qquad z \\sim \\mathcal{N}(0, I) \\text{ for } t&gt;1\n\\]\nOrdinarily \\(\\nabla_x \\log p_t\\) is what a trained network approximates. Here it’s the closed form from the previous section — nothing learned, nothing approximate.\n\nn_gen &lt;- 4000\nset.seed(11)\nx_gen &lt;- rnorm(n_gen)\nfor (t in T_steps:1) {\n  score_val &lt;- exact_score(x_gen, t)\n  noise_term &lt;- if (t &gt; 1) rnorm(n_gen) else 0\n  x_gen &lt;- (x_gen + beta[t] * score_val) / sqrt(alpha[t]) + sqrt(beta[t]) * noise_term\n}\n\nks_result &lt;- ks.test(x0_data, x_gen)\n\ndata.frame(x = c(x0_data, x_gen), source = rep(c(\"original data\", \"reverse-generated\"), each = n)) |&gt;\n  ggplot(aes(x, fill = source)) +\n  geom_histogram(aes(y = after_stat(density)), bins = 60, alpha = 0.55, position = \"identity\") +\n  labs(y = \"density\", fill = NULL)\n\n\n\n\n\n\n\nFigure 2: Samples generated by reverse diffusion from pure noise, using only the exact score, against the true starting distribution.\n\n\n\n\n\nGeneration recovers both modes: mean/sd of the lower cluster \\(-1.9774\\) / \\(0.3953\\) against a true \\(-2\\)/\\(0.4\\), and the upper cluster \\(1.9727\\) / \\(0.3994\\) against a true \\(2\\)/\\(0.4\\). A two-sample Kolmogorov-Smirnov test against the original data gives \\(D=0.023\\), \\(p=0.241\\) — nowhere near rejecting “same distribution.”"
  },
  {
    "objectID": "explainers/toy-diffusion-model/index.html#what-this-stands-in-for",
    "href": "explainers/toy-diffusion-model/index.html#what-this-stands-in-for",
    "title": "A Diffusion Model You Can Actually Solve",
    "section": "What this stands in for",
    "text": "What this stands in for\nThe diffusion-phase-transition post does exactly this same procedure — forward-diffuse partway, then reverse — but on real ImageNet images, with a trained U-Net standing in for the score computed here in closed form (Ho, Jain, and Abbeel 2020). The abrupt class-flip reported there is a direct consequence of how that (learned, not closed-form) score behaves across noise levels for hierarchically-structured data — the same mechanics as this toy, just with a far richer \\(p_0\\) than two Gaussian bumps (Sclocchi, Favero, and Wyart 2024)."
  },
  {
    "objectID": "explainers/toy-diffusion-model/index.html#series-wrap-up",
    "href": "explainers/toy-diffusion-model/index.html#series-wrap-up",
    "title": "A Diffusion Model You Can Actually Solve",
    "section": "Series wrap-up",
    "text": "Series wrap-up\nThree explainers, one underlying machine: a Markov chain moves data toward noise and, run in reverse with the right score function, moves noise back toward data; mean-field/annealed averaging is how you get a tractable equation for when that reversal preserves the “shape” of the data versus loses it; and the toy model here makes both pieces concrete enough to check by hand. With all three in place, Reproducing a Phase Transition in Diffusion Models and Fixed Points, Phase Transitions, and Diffusion Models should read as engineering and analysis built on genuinely solid ground, rather than machinery taken on faith."
  },
  {
    "objectID": "explainers/toy-diffusion-model/index.html#references",
    "href": "explainers/toy-diffusion-model/index.html#references",
    "title": "A Diffusion Model You Can Actually Solve",
    "section": "References",
    "text": "References\n\n\nHo, Jonathan, Ajay Jain, and Pieter Abbeel. 2020. “Denoising Diffusion Probabilistic Models.” arXiv Preprint arXiv:2006.11239.\n\n\nSclocchi, Antonio, Alessandro Favero, and Matthieu Wyart. 2024. “A Phase Transition in Diffusion Models Reveals the Hierarchical Nature of Data.” arXiv Preprint arXiv:2402.16991."
  },
  {
    "objectID": "papers/index.html",
    "href": "papers/index.html",
    "title": "Papers",
    "section": "",
    "text": "Original research papers.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nInstrumenting Agent Cybernetics\n\n\nOpen-World Estimation of Requisite Variety in Deployed Agents\n\n\n\nagents\n\ncybernetics\n\ninformation-theory\n\n\n\nAn open-world instrument for Ashby’s requisite variety in deployed AI agents: the Enumeration Elasticity Theorem, a feedback-blindness test, and a prompt-injection susceptibility measure.\n\n\n\n\n\nJuly 2026\n\n\nPaul Milne\n\n\n\n\n\n\n\n\n\n\n\n\nRG Functional Equations & Critical Phenomena\n\n\nin Thermodynamics and Financial Markets\n\n\n\nstatistical-mechanics\n\nrenormalization-group\n\nfinance\n\n\n\nA comparison of statistical-mechanics and functional-equation approaches to critical phenomena, applied to thermodynamics and financial market bubbles.\n\n\n\n\n\nJune 2022\n\n\nPaul Milne\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "papers/rg-critical-phenomena/index.html",
    "href": "papers/rg-critical-phenomena/index.html",
    "title": "RG Functional Equations & Critical Phenomena",
    "section": "",
    "text": "The paper considers the subject of critical phenomena in the physical world of thermodynamics and its analog in the economic world and the formation of bubbles in financial markets. Providing a review and comparison of a “bottom-up” approach based on statistical mechanics with a more “top-down” construction using iterative solutions to functional equations, both of which exhibit the required scale invariance (power laws) and fractal qualities seen near the critical points, and where the renormalization group theory provides a “bridge” between these two approaches both in a physical and mathematical sense.\nM840 Dissertation in Mathematics, The Open University.\nDownload the PDF\nRelated: Fixed Points, Phase Transitions, and Diffusion Models connects this dissertation’s renormalization-group critical phenomena to the phase transitions found in hierarchical diffusion models · Mean-Field Theory and Annealed Disorder walks through the Curie-Weiss mean-field derivation and its critical exponents."
  },
  {
    "objectID": "papers/rg-critical-phenomena/index.html#abstract",
    "href": "papers/rg-critical-phenomena/index.html#abstract",
    "title": "RG Functional Equations & Critical Phenomena",
    "section": "",
    "text": "The paper considers the subject of critical phenomena in the physical world of thermodynamics and its analog in the economic world and the formation of bubbles in financial markets. Providing a review and comparison of a “bottom-up” approach based on statistical mechanics with a more “top-down” construction using iterative solutions to functional equations, both of which exhibit the required scale invariance (power laws) and fractal qualities seen near the critical points, and where the renormalization group theory provides a “bridge” between these two approaches both in a physical and mathematical sense.\nM840 Dissertation in Mathematics, The Open University.\nDownload the PDF\nRelated: Fixed Points, Phase Transitions, and Diffusion Models connects this dissertation’s renormalization-group critical phenomena to the phase transitions found in hierarchical diffusion models · Mean-Field Theory and Annealed Disorder walks through the Curie-Weiss mean-field derivation and its critical exponents."
  },
  {
    "objectID": "papers/rg-critical-phenomena/index.html#read-it-here",
    "href": "papers/rg-critical-phenomena/index.html#read-it-here",
    "title": "RG Functional Equations & Critical Phenomena",
    "section": "Read it here",
    "text": "Read it here"
  },
  {
    "objectID": "papers/rg-critical-phenomena/index.html#cite-this-paper",
    "href": "papers/rg-critical-phenomena/index.html#cite-this-paper",
    "title": "RG Functional Equations & Critical Phenomena",
    "section": "Cite this paper",
    "text": "Cite this paper\n@mastersthesis{milne2022rg,\n  title        = {RG Functional Equations \\& Critical Phenomena in Thermodynamics and Financial Markets},\n  author       = {Milne, Paul},\n  year         = {2022},\n  month        = jun,\n  school       = {The Open University},\n  note         = {M840 Dissertation}\n}"
  },
  {
    "objectID": "posts/rg-diffusion-phase-transitions/index.html",
    "href": "posts/rg-diffusion-phase-transitions/index.html",
    "title": "Fixed Points, Phase Transitions, and Diffusion Models",
    "section": "",
    "text": "Note\n\n\n\nThis post’s comparative framing was originally drafted by an AI research assistant under my general direction, synthesizing my own 2022 dissertation and Sclocchi et al. (2024). The mathematics has been independently re-derived and computed in R below, rather than simply reproduced from that draft.\nTwo pieces of work on this blog turn out to share more mathematics than their subject headings suggest. My 2022 dissertation (Milne 2022) uses the renormalization group (RG) to describe critical phenomena in thermodynamics and financial markets. An earlier post reproduces Sclocchi, Favero & Wyart’s (2024) result (Sclocchi, Favero, and Wyart 2024) that denoising diffusion models undergo a sharp, hierarchy-dependent phase transition. Both are, underneath, the same story: a discrete iterated map, a critical parameter, and an unstable fixed point that separates two very different fates for the system.\nThis post makes that bridge concrete rather than just asserting it — implementing both maps in R, finding their fixed points numerically, and plotting the resulting cobweb diagrams side by side."
  },
  {
    "objectID": "posts/rg-diffusion-phase-transitions/index.html#two-hierarchies-one-structure",
    "href": "posts/rg-diffusion-phase-transitions/index.html#two-hierarchies-one-structure",
    "title": "Fixed Points, Phase Transitions, and Diffusion Models",
    "section": "Two hierarchies, one structure",
    "text": "Two hierarchies, one structure\nBoth frameworks simplify a high-dimensional system into a multi-scale tree. In statistical mechanics, a 2D spin lattice is grouped recursively into blocks, each decimated down to a single block-spin variable — Kadanoff’s original coarse-graining move. In Sclocchi et al.’s Random Hierarchy Model (RHM), the same tree runs the other way: a class label at the root branches down through latent features to the pixels at the leaves. The parameters line up directly:\n\n\n\n\n\n\n\nStatistical mechanics & RG\nHierarchical diffusion (RHM)\n\n\n\n\nMicroscopic spin configuration \\(s_i \\in \\{-1, 1\\}\\)\nLeaf / input variables \\(X_i^{(0)}\\)\n\n\nBlock-spin variable at level \\(\\ell\\)\nLatent feature node \\(X_i^{(\\ell)}\\)\n\n\nLattice spacing / scale length\nTree branching factor \\(s\\)\n\n\nCorrelation length \\(\\xi\\)\nCorrect reconstruction depth\n\n\nPotts state dimension \\(Q\\)\nAlphabet size \\(v\\)\n\n\nCoupling strength \\(K = J/kT\\)\nBelief in correct sequence \\(p = 1 - \\epsilon + \\epsilon/v\\)\n\n\nBond recursion under decimation\nProduction-rule density \\(f\\)\n\n\n\nIn both, one parameter controls order versus disorder: \\(K\\) above a critical value \\(K_c\\) gives spontaneous magnetic alignment; \\(f &lt; 1\\) in the RHM gives structured, hierarchically-correlated data rather than unstructured noise."
  },
  {
    "objectID": "posts/rg-diffusion-phase-transitions/index.html#the-rg-side-kadanoff-decimation-and-the-critical-coupling",
    "href": "posts/rg-diffusion-phase-transitions/index.html#the-rg-side-kadanoff-decimation-and-the-critical-coupling",
    "title": "Fixed Points, Phase Transitions, and Diffusion Models",
    "section": "The RG side: Kadanoff decimation and the critical coupling",
    "text": "The RG side: Kadanoff decimation and the critical coupling\nFor the Potts model on a hierarchical diamond lattice, decimating spin sites under a scale transformation gives an exact recurrence for the coupling strength \\(K\\):\n\\[\nK_{\\ell-1} = \\phi(K_\\ell) \\equiv \\left( \\frac{K_\\ell^2 + Q - 1}{2K_\\ell + Q - 2} \\right)^2\n\\]\nThe physical critical point — where the correlation length diverges — is the unstable fixed point of this map, \\(K_c = \\phi(K_c)\\). I’ll use \\(Q=3\\) (a three-state Potts model): it avoids a singularity in the denominator that shows up at \\(Q=2\\), and it happens to give a clean, exact critical point.\n\nphi &lt;- function(K, Q) ((K^2 + Q - 1) / (2 * K + Q - 2))^2\nQ &lt;- 3\n\nKc &lt;- uniroot(function(K) phi(K, Q) - K, interval = c(1.5, 10))$root\n\nh &lt;- 1e-6\ndphi &lt;- function(K) (phi(K + h, Q) - phi(K - h, Q)) / (2 * h)\n\ncurve_df &lt;- data.frame(K = seq(0.5, 6, length.out = 400)) |&gt;\n  mutate(phiK = phi(K, Q))\n\nfixed_points &lt;- data.frame(\n  K = c(1, Kc),\n  stability = c(\"stable\", \"unstable\")\n)\n\ncb_below &lt;- cobweb_path(phi, x0 = 3.9, n = 12, Q = Q) |&gt; mutate(traj = \"K0 = 3.9\")\ncb_above &lt;- cobweb_path(phi, x0 = 4.1, n = 6, Q = Q) |&gt; mutate(traj = \"K0 = 4.1\")\ncobweb_df &lt;- bind_rows(cb_below, cb_above)\n\nggplot() +\n  geom_abline(slope = 1, intercept = 0, linetype = \"dashed\", colour = \"grey50\") +\n  geom_line(data = curve_df, aes(K, phiK), linewidth = 1) +\n  geom_path(data = cobweb_df, aes(x, y, group = traj, colour = traj), linewidth = 0.5) +\n  geom_point(\n    data = fixed_points, aes(K, K, shape = stability, colour = stability),\n    size = 3, show.legend = c(shape = TRUE, colour = FALSE)\n  ) +\n  scale_shape_manual(values = c(stable = 16, unstable = 4)) +\n  coord_cartesian(xlim = c(0.5, 6), ylim = c(0.5, 6)) +\n  labs(\n    x = expression(K[ell]), y = expression(K[ell - 1] == phi(K[ell])),\n    colour = \"trajectory\", shape = \"fixed point\",\n    title = \"Kadanoff-Potts RG recursion (Q = 3)\"\n  )\n\n\n\n\n\n\n\nFigure 1: Kadanoff-Potts RG recursion (Q = 3). The unstable critical coupling sits at K_c = 4; trajectories starting on either side flow away from it toward one of two very different fates.\n\n\n\n\n\nSolving numerically: \\(K_c = 4\\), with derivative \\(\\phi'(K_c) \\approx 1.778\\) — greater than 1, confirming it’s unstable. A trajectory starting just below \\(K_c\\) (green line, \\(K_0=3.9\\)) flows down; one starting just above (orange, \\(K_0=4.1\\)) flows up and diverges.\nOne correction to the source material is worth flagging here. The original synthesis described the disordered basin as flowing toward \\(K \\to 0\\). Checking the map directly: \\(\\phi(0, 3) = 4\\), not \\(0\\) — so \\(K=0\\) isn’t a fixed point of this recursion at all. The actual trivial fixed point, true for every \\(Q\\), is \\(K=1\\): \\(\\phi(1, Q) \\equiv 1\\) identically, since numerator and denominator both reduce to \\(Q\\). It’s also superstable (\\(\\phi'(1) = 0\\)), which is why the “K0 = 3.9” trajectory above collapses onto it so fast."
  },
  {
    "objectID": "posts/rg-diffusion-phase-transitions/index.html#the-diffusion-side-belief-propagation-and-the-class-transition",
    "href": "posts/rg-diffusion-phase-transitions/index.html#the-diffusion-side-belief-propagation-and-the-class-transition",
    "title": "Fixed Points, Phase Transitions, and Diffusion Models",
    "section": "The diffusion side: belief propagation and the class transition",
    "text": "The diffusion side: belief propagation and the class transition\nSclocchi et al. solve the optimal denoising dynamics for the RHM with belief propagation, tracking the probability \\(p\\) of correctly reconstructing a feature at layer \\(\\ell\\):\n\\[\np' = F(p) \\equiv \\frac{p^s + f\\,\\frac{m-1}{mv-1}(1 - p^s)}{p^s + f(1 - p^s)}\n\\]\n\\(p^*=1\\) (perfect reconstruction) and \\(p^*=1/v\\) (chance level) are the two trivial regimes. Whether \\(p^*=1\\) is attractive or repulsive depends on the product \\(sf\\) through \\(F'(1) \\approx sf\\) — and that’s the whole ballgame: below the threshold, denoising can recover the class; above it, it can’t.\n\nF_bp &lt;- function(p, s, f, m, v) {\n  c_ &lt;- (m - 1) / (m * v - 1)\n  (p^s + f * c_ * (1 - p^s)) / (p^s + f * (1 - p^s))\n}\n\ns &lt;- 2\nm &lt;- 3\nv &lt;- 10\nphases &lt;- data.frame(\n  phase = c(\"Order phase (f = 0.3)\", \"Disorder phase (f = 0.8)\"),\n  f = c(0.3, 0.8)\n)\n\nfind_fp &lt;- function(f) {\n  g &lt;- function(p) F_bp(p, s, f, m, v) - p\n  grid &lt;- seq(1e-4, 1 - 1e-6, length.out = 4000)\n  vals &lt;- vapply(grid, g, numeric(1))\n  idx &lt;- which(diff(sign(vals)) != 0)\n  roots &lt;- if (length(idx) &gt; 0) {\n    vapply(idx, function(i) uniroot(g, grid[i:(i + 1)])$root, numeric(1))\n  } else {\n    numeric(0)\n  }\n  sort(unique(round(c(roots, 1), 6)))\n}\nstability_of &lt;- function(p, f) {\n  hh &lt;- 1e-6\n  d &lt;- (F_bp(min(p + hh, 1 - 1e-9), s, f, m, v) -\n    F_bp(max(p - hh, 1e-9), s, f, m, v)) / (2 * hh)\n  ifelse(d &gt; 1, \"unstable\", \"stable\")\n}\n\np_grid &lt;- seq(0.001, 1, length.out = 400)\ncurve_df &lt;- bind_rows(lapply(seq_len(nrow(phases)), function(i) {\n  data.frame(\n    p = p_grid, Fp = F_bp(p_grid, s, phases$f[i], m, v),\n    phase = phases$phase[i]\n  )\n}))\n\nfp_df &lt;- bind_rows(lapply(seq_len(nrow(phases)), function(i) {\n  roots &lt;- find_fp(phases$f[i])\n  data.frame(\n    p = roots, stability = sapply(roots, stability_of, f = phases$f[i]),\n    phase = phases$phase[i]\n  )\n}))\n\ncobweb_df &lt;- bind_rows(\n  cobweb_path(F_bp, 0.20, s = s, f = 0.3, m = m, v = v) |&gt;\n    mutate(traj = \"p0 = 0.20\", phase = phases$phase[1]),\n  cobweb_path(F_bp, 0.50, s = s, f = 0.3, m = m, v = v) |&gt;\n    mutate(traj = \"p0 = 0.50\", phase = phases$phase[1]),\n  cobweb_path(F_bp, 0.95, s = s, f = 0.8, m = m, v = v) |&gt;\n    mutate(traj = \"p0 = 0.95\", phase = phases$phase[2]),\n  cobweb_path(F_bp, 0.50, s = s, f = 0.8, m = m, v = v) |&gt;\n    mutate(traj = \"p0 = 0.50\", phase = phases$phase[2])\n)\n\ncurve_df &lt;- curve_df |&gt; mutate(phase = factor(phase, levels = phases$phase))\nfp_df &lt;- fp_df |&gt; mutate(phase = factor(phase, levels = phases$phase))\ncobweb_df &lt;- cobweb_df |&gt; mutate(phase = factor(phase, levels = phases$phase))\n\nggplot() +\n  geom_abline(slope = 1, intercept = 0, linetype = \"dashed\", colour = \"grey50\") +\n  geom_line(data = curve_df, aes(p, Fp), linewidth = 1) +\n  geom_path(data = cobweb_df, aes(x, y, group = traj), colour = \"steelblue\", linewidth = 0.4) +\n  geom_point(data = fp_df, aes(p, p, shape = stability, colour = stability), size = 3) +\n  scale_shape_manual(values = c(stable = 16, unstable = 4)) +\n  scale_colour_manual(values = c(stable = \"#2C7FB8\", unstable = \"#D95F02\")) +\n  facet_wrap(~phase) +\n  labs(x = \"Probability p at layer l\", y = \"Probability p' at layer l+1\", colour = \"fixed point\", shape = \"fixed point\")\n\n\n\n\n\n\n\nFigure 2: Belief-propagation map F(p). Order phase: an unstable fixed point separates two stable attractors – a sharp phase transition in noise space. Disorder phase: the noiseless state itself becomes unstable, and every trajectory decays to the noise floor.\n\n\n\n\n\nWith \\(s=2\\), \\(m=3\\), \\(v=10\\): the order phase (\\(f=0.3\\)) has fixed points at \\(p \\approx 0.098\\) (stable), \\(p_c \\approx 0.301\\) (unstable), and \\(p=1\\) (stable) — a genuine bifurcation. Push \\(f\\) up to \\(0.8\\) and \\(p=1\\) flips to unstable, leaving only the noise floor at \\(p \\approx 0.076\\): every trajectory, however close to perfect reconstruction it starts, decays away.\nTwo honesty notes. First, \\(p=1/v=0.1\\) isn’t an exact algebraic root of \\(F\\) for arbitrary parameters — I checked this numerically rather than assuming it, and it only holds exactly for special relationships between \\(m\\), \\(v\\), \\(s\\), \\(f\\). The stable floor above (\\(\\approx 0.098\\) / \\(\\approx 0.076\\)) sits close to \\(1/v\\) but isn’t pinned to it. Second, \\(F'(1) \\approx sf\\) is itself an approximation: the exact derivative carries a prefactor \\(\\frac{m(v-1)}{mv-1}\\), which here is \\(0.931\\) — so the true values are \\(0.559\\) and \\(1.49\\) rather than exactly \\(0.6\\) and \\(1.6\\). Both still land on the correct side of \\(1\\), which is all the theory actually needs.\n\\(s\\), \\(m\\), and \\(v\\) here were chosen to land close to the illustrative numbers in the original comparison, not pulled from Sclocchi et al.’s own experiments — treat them as a worked example of the mechanism, not a replication of their measured parameters."
  },
  {
    "objectID": "posts/rg-diffusion-phase-transitions/index.html#same-bifurcation-two-dictionaries",
    "href": "posts/rg-diffusion-phase-transitions/index.html#same-bifurcation-two-dictionaries",
    "title": "Fixed Points, Phase Transitions, and Diffusion Models",
    "section": "Same bifurcation, two dictionaries",
    "text": "Same bifurcation, two dictionaries\nBoth maps are governed by the identical mechanism: a system crosses the basin boundary set by an unstable fixed point of a nonlinear discrete map, and the outcome on the other side is qualitatively different from where it started. A magnet losing its alignment, a market crashing, and a diffusion model’s reconstructed class flipping to something else entirely are, in this sense, the same event described in three different vocabularies."
  },
  {
    "objectID": "posts/rg-diffusion-phase-transitions/index.html#schröders-equation-the-linearizing-bridge",
    "href": "posts/rg-diffusion-phase-transitions/index.html#schröders-equation-the-linearizing-bridge",
    "title": "Fixed Points, Phase Transitions, and Diffusion Models",
    "section": "Schröder’s equation: the linearizing bridge",
    "text": "Schröder’s equation: the linearizing bridge\nBoth works lean on the same piece of machinery for handling these maps analytically: Schröder’s functional equation (Curtright and Zachos 2011),\n\\[\n\\psi(\\phi(x)) = \\lambda\\, \\psi(x)\n\\]\nwhere \\(\\lambda = \\phi'(x^*)\\) is the eigenvalue at a fixed point and \\(\\psi\\) is the (unknown) conjugating function. It linearizes the messy nonlinear dynamics of \\(\\phi\\) into simple scaling by \\(\\lambda\\) near \\(x^*\\) — which is what lets you extend a discrete iteration into a continuous-time trajectory, \\(x(t) = \\psi^{-1}(\\lambda^t \\psi(x))\\).\n\nA brief aside: the Johansen–Ledoit–Sornette crash model\nIn my dissertation, applying this to the Johansen-Ledoit-Sornette model of financial bubbles — where the scaling factor \\(\\lambda\\) is complex rather than real — gives the Log-Periodic Power Law:\n\\[\nF(x) = x^\\beta\\, G(\\omega \\log x)\n\\]\nwith \\(G\\) periodic and \\(x = |t_c - t|\\) the distance from the crash time \\(t_c\\) (Johansen, Ledoit, and Sornette 1998). The oscillations get faster (in \\(x\\)) as the crash approaches, which is the model’s signature prediction of accelerating, log-periodic herding behaviour before a crash.\n\nlppl &lt;- function(x, beta = 0.3, A = 1, B = 0.3, omega = 6, phase = 0) {\n  x^beta * (A + B * cos(omega * log(x) + phase))\n}\ndata.frame(x = seq(0.001, 1, length.out = 2000)) |&gt;\n  mutate(Fx = lppl(x)) |&gt;\n  ggplot(aes(x, Fx)) +\n  geom_line() +\n  scale_x_reverse() +\n  labs(\n    x = expression(\"|\" * t[c] - t * \"|\" ~ \"(time to crash, decreasing rightward)\"),\n    y = \"F(x)  (illustrative index level)\"\n  )\n\n\n\n\n\n\n\nFigure 3: Illustrative log-periodic power law approaching a critical time – not fitted to real market data, just the functional form.\n\n\n\n\n\nSclocchi et al. don’t solve an analogue of this — but they make a structural observation that plays a similar bridging role. The convolutional U-Net used for denoising naturally approximates belief propagation: the encoder path acts as the upward BP pass (coarse-graining noisy pixels into abstract features), and the decoder path with its skip connections acts as the downward pass, mixing global class belief back down with local detail to reconstruct clean pixels."
  },
  {
    "objectID": "posts/rg-diffusion-phase-transitions/index.html#where-the-analogy-breaks-scale-invariance",
    "href": "posts/rg-diffusion-phase-transitions/index.html#where-the-analogy-breaks-scale-invariance",
    "title": "Fixed Points, Phase Transitions, and Diffusion Models",
    "section": "Where the analogy breaks: scale invariance",
    "text": "Where the analogy breaks: scale invariance\nThe two frameworks diverge on one point worth stating plainly rather than glossing over. In the RG picture, criticality brings true continuous scale invariance: the correlation length diverges, the lattice spacing becomes irrelevant, and the system is fractally self-similar — which is exactly why wildly different physical materials fall into the same universality class.\nDiffusion models don’t have this. Scale invariance is broken during denoising: the macroscopic scale (the class label) undergoes a sharp, discontinuous transition at a critical noise level, while the microscopic scale (fine texture) changes smoothly and continuously straight through that transition — sometimes composing pieces of the old class with the new one (a wolf generated with leopard eyes, in Sclocchi et al.’s example). That’s a time-scale separation reflecting the discrete, compositional structure of real data, not the continuous scale invariance of a system at a genuine critical point. The mathematics of the fixed point is shared; the physical picture it’s describing isn’t quite the same."
  },
  {
    "objectID": "posts/rg-diffusion-phase-transitions/index.html#conclusion",
    "href": "posts/rg-diffusion-phase-transitions/index.html#conclusion",
    "title": "Fixed Points, Phase Transitions, and Diffusion Models",
    "section": "Conclusion",
    "text": "Conclusion\nKadanoff’s block-spin decimation and Sclocchi et al.’s belief propagation on the Random Hierarchy Model turn out to be the same functional-equation machinery pointed at two different problems — one asking how magnetic order emerges from a spin lattice, the other how a diffusion model recovers (or loses) a semantic class from noise. See the dissertation for the full RG derivation, and the earlier post for the diffusion side reproduced end to end on real images.\nRelated: Mean-Field Theory and Annealed Disorder is a refresher on the annealed approximation this post’s belief-propagation map relies on, worked through the classical Curie-Weiss ferromagnet."
  },
  {
    "objectID": "posts/rg-diffusion-phase-transitions/index.html#references",
    "href": "posts/rg-diffusion-phase-transitions/index.html#references",
    "title": "Fixed Points, Phase Transitions, and Diffusion Models",
    "section": "References",
    "text": "References\n\n\nCurtright, Thomas L., and Cosmas K. Zachos. 2011. “Renormalization Group Functional Equations.” Physical Review D 83: 065019. https://doi.org/10.1103/PhysRevD.83.065019.\n\n\nJohansen, Anders, Olivier Ledoit, and Didier Sornette. 1998. “Crashes as Critical Points.” arXiv Preprint Cond-Mat/9810071.\n\n\nMilne, Paul. 2022. “RG Functional Equations & Critical Phenomena in Thermodynamics and Financial Markets.” Master’s thesis, The Open University.\n\n\nSclocchi, Antonio, Alessandro Favero, and Matthieu Wyart. 2024. “A Phase Transition in Diffusion Models Reveals the Hierarchical Nature of Data.” arXiv Preprint arXiv:2402.16991."
  }
]