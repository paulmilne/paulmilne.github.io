.cache/

/.quarto/

# rendered site output (regenerate with `quarto render`)
/_site/

# RStudio / R session artifacts
.Rproj.user/
.Rhistory
.RData
.RDataTmp

# self-contained research working tree with its own git repo, model
# checkpoints, datasets, and a Python venv -- not blog content
/dl/

.DS_Store

# knitr chunk caches (regenerable, can get large; use _freeze/ for reproducibility instead)
*_cache/

# source PDF dropped at repo root before being organized under papers/
/M840-Dissertation-Paul_Milne-Final.pdf
